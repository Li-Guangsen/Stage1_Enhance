# 研究日志

按时间顺序记录研究决策与行动，仅追加，不回写历史条目。

| 编号 | 日期 | 类型 | 摘要 |
|---|------|------|---------|
| 1 | 2026-04-11 | 启动 | 在 `D:\\Desktop\\Stage1Codex` 初始化 autoresearch 工作区，创建 research-state/log/findings 文件，并为本线程设置 20 分钟连续性心跳。 |
| 2 | 2026-04-11 | 启动 | 通读并对齐当前仓库代码（`main.py`, `lgsbph.py`, `pybemd.py`, `RGHS.py`, `CLAHE.py`, `fusion_three.py`, `lvbo.py`）、两篇本地参考论文以及既有项目指引日志。 |
| 3 | 2026-04-11 | 启动 | 确认当前工作方法是七阶段增强流水线：改进的灰像素引导白平衡 → IMF1Ray 细节分支 → RGHS 对比分支 → CLAHE 分支 → 特征门控三分支融合 → 同态细化。 |
| 4 | 2026-04-11 | 启动 | 围绕灰像素引导 clipped ACCC、特征门控融合和同态细化形成 H1-H3 初始假设，并在进一步扩展架构前锁定当前项目框架为“增强方法 + 下游边缘验证”。 |
| 5 | 2026-04-11 | 启动 | 将两篇本地论文记录为首批关键文献，并标记当前证据缺口：标准化评测子集、锁定的消融协议，以及更强的下游边缘验证实验。 |
| 6 | 2026-04-11 | 启动 | 围绕当前已完成增强结果 `results/png/Final` 定义的 92 张 pilot 子集，锁定第一版受控评测协议 `pilot92-v1`。 |
| 7 | 2026-04-11 | 启动 | 将消融优先级锁定为 H2（fusion）→ H1（white balance）→ H3（homomorphic refinement），因为 H2 在当前仓库里既是最强的代码级新意，也是最干净的首个实验入口。 |
| 8 | 2026-04-11 | 文献 | 为综述补充两篇邻近参考：2023 年 EMD-wavelet HAB enhancement 论文和 2024 年 Frontiers HAB edge-detection 论文。 |
| 9 | 2026-04-11 | 汇报 | 在 `to_human/` 生成 bootstrap 进度报告，方便人工在不读原始笔记的情况下查看当前研究状态。 |
| 10 | 2026-04-12 | 实验 | 新增 protocol-v2 评测/打分脚本，对 `main.py` 做参数化，并在 `pilot92-v1` 上评测当前各阶段输出，确认 `Final` 是现有阶段中的最佳输出。 |
| 11 | 2026-04-12 | 实验 | 基于现有 BPH/IMF1Ray/RGHS/CLAHE PNG 运行 17 个后阶段融合/细化候选搜索，选出 `c25_rghs_entropy_mid` 作为平衡型 pilot 优胜者，因为它在几乎追平最高综合分的同时，把 Mean_MS_SSIM 保持在 0.85 以上。 |
| 12 | 2026-04-13 | 实验 | 将调参切换到全量 506 张 `Original` 数据集。完成四轮 full506 后阶段细化搜索后，选出 `r4_03_gamma_050_210_48` 作为当前综合指标优胜者；其相对 `c25` 在 506/506 complete cases、零失败条件下表现更优，并将可复现实验配置保存到 `experiments/optimization_v1/configs/best_full506_r4_03.json`。 |
| 13 | 2026-04-13 | 实验 | 围绕固定下游 `r4_03` 实现 H1 白平衡调参 protocol v2：新增 `experiments/h1-graypixel-bph-ablation/run_bph_search.py`，将 H1 协议切换为面向 full506 的 explore64 → round1 → round2 → full506 编排，并把结构护栏和可视化诊断编码进整条流水线。 |
| 14 | 2026-04-13 | 实验 | 从全部 506 张原图构建分层 `explore64` manifest（`data/eval_subset_explore64_full506_bph_v1.txt`），并在 `bph00_baseline` 与 `bph10_gp_strict` 上完成 5 张样本的 smoke run。六阶段链路全部执行成功，clean protocol-v2 评测给出 5/5 complete cases、零失败。 |
| 15 | 2026-04-15 | 实验 | 完成 H1 白平衡 `full506` 正式实验，结果位于 `experiments/h1-graypixel-bph-ablation/outputs/full506`：四套方法全部在 `506/506 complete-case` 图像上完成评测、无失败、诊断图生成成功，且 `selection.json` 将 `r2_02_G_P` 记录为当前 metric winner。三个白平衡候选相对 `r2_00_baseline` 都通过了硬护栏和动态护栏；人工抽看诊断图未见明显系统性偏色，因此当前建议将 `r2_02_G_P` 锁定为 H1 白平衡赢家。 |
| 16 | 2026-04-15 | 决策 | 在保留自动评测结果 `metric_winner = r2_02_G_P` 的前提下，人工最终将 `r2_05_G_P_A_B` 锁定为 H1 白平衡正式胜者，并作为后续上游白平衡调参默认配置。锁定理由是：它相对 `r2_00_baseline` 同时提升了 `MS-SSIM` 与 `PSNR`，且在“双增”候选中综合分回撤最小，适合作为更稳的前置白平衡方案。 |
| 17 | 2026-04-15 | 文档 | 为当前主线补充显式锁定配置，最初用于统一表示“H1 `r2_05_G_P_A_B` + 下游 `r4_03`”组合；同时更新中文 README 与实验分析文档，明确该类锁定文件用于复现和引用，但不自动改写 `main.py` 的默认参数入口。该临时命名方案随后在同日整理为正式名称 `locked_full506_mainline.json`。 |
| 18 | 2026-04-15 | 文档 | 将主线锁定配置正式整理为 `experiments/optimization_v1/configs/locked_full506_mainline.json`，并把已确认结果复制到 `experiments/h1-graypixel-bph-ablation/outputs/full506/runs/full506_locked_mainline` 与 `candidate_params/full506_locked_mainline.json`。新的正式命名用于后续展示和文档引用；历史候选命名保留在评测和诊断文件中，用于审计链回溯。 |
| 19 | 2026-04-17 | 文档 | 将 `RGHS` 分支主实现文件从 `RGHS.py` 重命名为 `wb_safe_contrast.py`，以匹配其真实功能“白平衡安全对比增强分支”。阶段名 `RGHS` 仍保留在结果目录、配置字段和实验记录中；`RGHS.py` 仅作为兼容旧导入的薄封装保留。 |
| 20 | 2026-04-17 | 文档 | 将 `CLAHE` 分支主实现文件从 `CLAHE.py` 重命名为 `clahe_guided_visibility.py`，以匹配其真实功能“CLAHE 引导的局部可见性增强分支”。阶段名 `CLAHE` 仍保留在结果目录、配置字段和实验记录中；`CLAHE.py` 仅作为兼容旧导入的薄封装保留。 |
| 21 | 2026-04-17 | 文档 | 新增本地方法草稿 `method-underwater-enhancement.md`，按当前代码真实实现写出 method section 的中文完整版、中文短版与英文骨架，并同步到 Notion `论文写作` 数据库条目《水下图像增强 method section 草稿》；同时将该条目加入项目主页的当前重点页面。 |
| 22 | 2026-04-17 | 文档 | 在方法草稿基础上继续压缩出更接近论文成稿的版本 `method-underwater-enhancement-paper-ready.md`，整理出正式的 `3 Method` 小节结构、英文骨架、图注草稿与实验过渡句，并同步到 Notion `论文写作` 数据库条目《水下图像增强 method section 精简稿》；同时将其加入项目主页的当前重点页面。 |
| 23 | 2026-04-17 | 文档 | 新增方法总览图说明 `method-figure-underwater-enhancement.md`，统一方法图的结构布局、节点命名、应强调与应避免暗示的内容、图注草稿与正文引用句，并同步到 Notion `论文写作` 数据库条目《水下图像增强 方法总览图说明》；同时将其加入项目主页的当前重点页面。 |
| 24 | 2026-04-17 | 文档 | 将方法总览图说明进一步落成实际图稿：新增 Mermaid 源文件 `paper/figures/underwater_method_overview.mmd`，并成功导出 `paper/figures/underwater_method_overview.svg` 与 `paper/figures/underwater_method_overview.png`；同时同步到 Notion `论文写作` 数据库条目《水下图像增强 方法总览图初稿》，并加入项目主页重点页面。 |
| 25 | 2026-04-17 | 文档 | 为便于当前版本先提交，新增更轻量的图稿 `paper/figures/underwater_method_overview_simple.mmd`，并导出 `paper/figures/underwater_method_overview_simple.svg` 与 `paper/figures/underwater_method_overview_simple.png`。该版本保留方法主链、三分支、融合中心和外部下游验证路径，但去掉了框内细节说明，适合作为先行提交和后续微调底稿。 |
| 26 | 2026-04-17 | 文献 | 新增 `related-work-underwater-enhancement.md`，围绕当前 Zotero 阅读集整理传统水下增强、HAB 显微增强谱系、深度模型以及白平衡/直方图建模的相关工作比较表、中文综述段落、英文骨架和短版写法，作为论文相关工作起草底稿。 |
| 27 | 2026-04-17 | 文档 | 统一补齐项目记录层：将 README、`research-state.yaml`、H2 中间阶段分析文档与当前接受主线对齐，明确 `locked_full506_final_mainline.json` 为当前接受配置，并把 method / related work / 方法图资产纳入仓库级文档导航。 |
| 28 | 2026-04-17 | 文档 | 为后续新线程可直接接手，在 `README.md` 中补充“新线程快速接手 / 历史工作记录摘要”，集中说明当前接受主线、已完成实验、已产出写作资产、Notion / Skill / MCP 的实际使用方式，以及新线程默认应先读哪些本地文件。 |
| 29 | 2026-04-23 | 论文 | 形成中文论文底稿与证据包的主体内容，整理当前增强主线、正式评测口径、相关工作、方法叙事、结果解释和审稿风险，主要落到 `paper/underwater_image_enhancement_draft_cn.md` 与 `paper/underwater_image_enhancement_evidence_pack_cn.md`。 |
| 30 | 2026-04-24 | 实验 | 将正式评测口径收敛为两层：`full502_clean_v1` 用于阶段进度表，`compare9_complete496_v1` 用于 `Ours + 8 baselines` 主比较表；正式输出保存到 `metrics/outputs/evaluate_protocol_v2/official_stage_progress_full502` 与 `metrics/outputs/evaluate_protocol_v2/official_compare9_complete496`。 |
| 31 | 2026-04-24 | 文档 | 新增并更新中文交接与状态文档，包括 `docs/project_handoff_guide_cn.md`、`docs/project_status_overview_cn.md` 和 `docs/comparison_methods_results_index_cn.md`，明确正式主线、正式结果入口、外部对比方法路径、complete-case 口径以及后续缺口。 |
| 32 | 2026-04-24 | 文档 | 更新 `README.md` 与 `research-state.yaml`，将当前状态标记为“正式主线已锁定、正式阶段评测与正式外部主表评测已完成”，并把写作语言优先级明确为中文主稿优先、英文 outline 和辅助稿作为后续资产保留。 |
| 33 | 2026-04-24 | 文档 | 按中文优先规则整理根目录写作草稿：将 `related-work-underwater-enhancement.md`、`method-underwater-enhancement.md`、`method-figure-underwater-enhancement.md` 和 `method-underwater-enhancement-paper-ready.md` 的标题、说明、表头、写作目标和注意事项改为中文主导，仅保留论文题名、方法缩写和明确标注的英文辅助稿。 |
| 34 | 2026-04-24 | 文档 | 新增项目级智能体执行规则入口 `AGENTS.md` 与完整规则文档 `docs/project_execution_rules_cn.md`，把“本地版本化文件才是智能体可见记忆”、正式主线/评测口径、中文主稿优先、实验重跑、论文解释边界和状态维护要求固化为下次接手必须遵守的规则；同时更新 `README.md` 阅读顺序和 `research-state.yaml` 规则索引。 |
| 35 | 2026-04-24 | 文档 | 整理旧版根目录 `findings.md`：确认其为 2026-04-11 autoresearch 初始化时生成的英文早期快照，内容已过时且不应继续作为当前入口；将英文原文归档到 `docs/archive-en/findings.en.md`，新增当前中文发现文档 `docs/project_findings_cn.md`，并更新 `research-state.yaml` 的 findings 索引。 |
| 36 | 2026-04-24 | 文档 | 标注更多历史遗留入口：在 `AIlog/图像增强论文指导.md`、`notion_mirror/README.md`、`experiments/h2-fusion-ablation/protocol.md`、`experiments/h3-homomorphic-refinement/protocol.md` 和 `experiments/optimization_v1/analysis.md` 顶部补充中文历史资料/历史协议说明；同时更新 `README.md` 与 `docs/project_handoff_guide_cn.md` 的历史资产清单，避免后续智能体把早期 bootstrap、Notion 镜像或 `pilot92-v1` 草案误当当前正式入口。 |
| 37 | 2026-04-26 | 文档 | 在 `AGENTS.md` 和 `docs/project_execution_rules_cn.md` 中新增多代理使用规则：复杂长任务可采用主代理 + 子代理模式，但主代理必须负责入口规则读取、任务拆分、正式口径限定、子代理结论汇总和最终验收；子代理仅处理边界清晰的局部读取、核对或复核任务，不能单独改变项目状态。 |
| 38 | 2026-04-26 | 文档 | 根据 OpenAI Codex 官方 Subagents 与 Sample Configuration 文档，补充项目多代理数量与深度规则：默认每轮 `1-3` 个直接子代理，明确需要大规模并行复核时最多 `6` 个，深度保持 `1`，不允许子代理继续递归派生；若未来写入项目级 Codex 配置，建议保持 `[agents] max_threads <= 6`、`max_depth = 1`。 |
| 39 | 2026-05-24 | 论文 | 执行中文投稿底稿收口计划：在证据包中新增“主张-证据-边界”矩阵并将 Notion、聊天历史、在线页面和未落盘事实降级为不可直接引用的事实源；将中文主稿 Part H 明确为当前收口底稿；新增 `paper/paper_ready_figures_plan_cn.md` 与 `docs/downstream_edge_validation_protocol_cn.md` 两份只规划、不产出新结果的投稿准备入口；同步更新 README、状态总览、交接指南和 `research-state.yaml`。本轮未运行实验、未生成新图表、未覆盖正式结果。 |
| 40 | 2026-05-24 | 论文 | 按当前 goal 将 `paper/underwater_image_enhancement_draft_cn.md` 从早期 Part E / Part H 双版本草稿收束为单一中文投稿主稿 v1.0，统一摘要、引言、贡献、方法、正式协议、阶段表、外部主表、讨论与结论口径；同步在 README、状态总览、证据包和 `research-state.yaml` 标注当前主稿状态。本文档修改未运行实验、未生成新图表、未覆盖正式结果。 |
| 41 | 2026-05-24 | 结果 | 面向 Stage1 与 MyEdge 衔接生成无 GT 边缘结构代理支撑包：新增 `metrics/scripts/run_downstream_edge_proxy_stage1.py`，使用 `full502_clean_v1` 完成阶段级 `502/502` 代理统计，使用 `compare9_complete496_v1` 完成外部增强方法级 `496/496` 代理统计，并生成 `summary.md/csv/json`、`per_image_metrics.csv` 与候选 qualitative panels 到 `metrics/outputs/downstream_edge_validation/official_full502_mainline`。同步新增 `docs/stage1_enhancement_to_edge_support_cn.md`，更新下游协议、状态总览、交接指南、对比方法索引、证据包、中文主稿和 `research-state.yaml`。该结果不替代 MyEdge / DiffusionEdge 带 GT 的 ODS/OIS/AP/AC 主实验，未覆盖正式增强结果目录。 |
| 42 | 2026-05-24 | 文献 | 修正旧文献条目：将 `literature/frontiers2024_enhanced_edge_detection_hab.md` 重命名为 `literature/wu2026_eswa_hab_edge_detection.md`，并根据 Zotero 条目 `9MCJDQGE` / `AKGYHG77` 更新为 `Expert Systems with Applications` 2026, Vol. 298, Article 129663, DOI `10.1016/j.eswa.2025.129663`。同步更新 `research-state.yaml` 中的 key paper id、年份、期刊、DOI、Zotero key 和项目约束说明。未运行实验、未修改正式结果。 |
| 43 | 2026-05-24 | 文档 | 综合 `D:\Desktop\suggestion-1.md`、`suggestion-2.md`、`suggestion-3.md` 三份建议，新增 `docs/stage1_myedge_three_suggestions_synthesis_cn.md`，明确总体论文应以 MyEdge / MSFI 为主创新，Stage1Codex 作为 `task-driven structure-preserving input formation` 支撑包；新增 `literature/wu2026_eaai_hab_segmentation.md` 并在 `research-state.yaml` 记录 EAAI 2026 核心对标论文 `ZMUBCGCD`；同步更新 README、状态总览、交接指南、中文主稿和证据包。该动作仅落实策略与写作边界，未运行实验、未生成新结果、未覆盖正式结果。 |
| 44 | 2026-05-24 | 数据 | 审计外部完整增强图像池 `D:\Desktop\去水印所有藻类图像`，生成候选口径 `full_algae_dewatermark_v1`：全目录图像 `2777` 张、默认候选 manifest `2774` 张、顶层数据文件夹 `80` 个、根目录说明图 `3` 张默认排除。新增 `docs/full_enhancement_dataset_inventory_cn.md` 与 `docs/stage1_myedge_long_term_closure_plan_cn.md`，并同步 README、状态总览、交接指南、执行规则和 `research-state.yaml`。该动作只生成 manifest / inventory / 计划文档，未运行增强实验、未重跑评测、未覆盖正式 502/496 结果。 |
| 45 | 2026-05-24 | 数据 | 新增并运行 `metrics/scripts/audit_image_manifest_decode.py`，对 `full_algae_dewatermark_v1` 的 `2777` 张图像完成 OpenCV decode / dimension / channel / dtype 审计：全量图像 `2773/2777` 可读，默认候选图像 `2770/2774` 可读，缺失文件 `0`；4 个候选失败文件均为 `.jpg` 扩展但 `GIF89a` 内容。生成 `full_algae_dewatermark_v1_decode_audit.*`、`full_algae_dewatermark_v1_cv2_readable_candidate.txt` 和 `full_algae_dewatermark_v1_decode_failures.tsv`，并同步数据审计文档、长周期计划、README、状态总览、交接指南、执行规则和 `research-state.yaml`。该动作为数据审计，不运行增强实验、不重跑评测、不覆盖正式 502/496 结果。 |
| 46 | 2026-05-24 | 代码 | 修正 `main.py` 的完整图像池 I/O 支持：新增中文路径安全读写（`np.fromfile + cv2.imdecode`、`cv2.imencode + tofile`）、`.webp` 输入扩展、保留空格和 `#` 的 manifest 解析、相对路径 manifest 解析以及嵌套输出目录，避免完整图像池中重名 stem 覆盖。已通过 `py_compile`，并静态验证 `full502_clean_v1` 仍解析为 `502` 张、`full_algae_dewatermark_v1_cv2_readable_candidate.txt` 解析为 `2770` 张，首张中文路径图像可读。本轮未运行 Stage1 full-pool 增强 smoke、未重跑评测、未覆盖正式 502/496 结果。 |
| 47 | 2026-05-24 | 实验 | 使用 `full_algae_dewatermark_v1_cv2_readable_candidate.txt`、外部源目录 `D:\Desktop\去水印所有藻类图像` 和锁定配置 `experiments/optimization_v1/configs/locked_full506_final_mainline.json` 完成 Stage1 full-pool smoke：`smoke_limit1_locked_final_mainline` 六阶段 JPG/PNG 各 `1` 个文件完整，`smoke_limit10_locked_final_mainline` 六阶段 JPG/PNG 各 `10` 个文件完整，Final PNG `10/10` 可解码为 `320x320x3`。第一次 10 张运行曾因 5 分钟工具超时中断，续跑使用 `--skip-existing` 补齐；本轮未运行 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 48 | 2026-05-24 | 文档 | 基于 10 张 smoke 输出补齐 2770 张 full run 预算和可恢复运行入口：新增 `experiments/full-algae-dewatermark-v1/run_budget_estimate.md`，估算 2770 张耗时约 `46-60` 小时、六阶段 JPG/PNG 输出约 `3.04 GiB`，建议至少预留 `5 GiB`；新增 `experiments/full-algae-dewatermark-v1/run_full_cv2readable2770_locked.ps1` 作为默认 `--skip-existing` 的可恢复长跑脚本；新增 `metrics/scripts/summarize_stage1_run_outputs.py` 并在 10 张 smoke 上验证 `all_complete=true`。同步 protocol、smoke summary、README、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 49 | 2026-05-25 | 文档 | 补齐 Stage1 2770 张 full-pool 长跑的接收链路：更新 `experiments/full-algae-dewatermark-v1/run_full_cv2readable2770_locked.ps1`，未来运行时会写入 `logs/full2770_locked_final_mainline.log`；新增 `metrics/scripts/intake_stage1_fullpool_run_outputs.py`，只读检查 manifest、六阶段 JPG/PNG 输出、日志、run report 和 Final PNG 抽样解码。已运行接收脚本生成 `full2770_locked_final_mainline_intake_status_20260525.{md,json}`，当前状态为 `not_started`，输出根目录不存在，预期 `2770` 张 / `33240` 个输出文件，当前存在 `0` 个。同步 protocol、README、状态总览、交接指南、执行规则、完整图像池审计、长周期计划和 `research-state.yaml`。本轮未启动 2770 张完整增强、未运行 `main.py`、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 50 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/audit_image_duplicates.py`，对 `full_algae_dewatermark_v1` 的 `2777` 张图像完成严格重复与近重复候选审计：全量 `2777/2777` 可生成 hash，其中 `2773` 张由 OpenCV 解码，4 个 `GIF89a` 内容文件由 Pillow fallback 解码；发现严格文件重复 `3` 组 / `6` 张候选图像，灰度图重复 `3` 组 / `6` 张，按 `pHash<=4` 且 `dHash<=8` 得到近重复候选 `30` 对。生成 `full_algae_dewatermark_v1_duplicate_audit.*`、严格重复组和近重复候选对 TSV，并同步 README、状态总览、交接指南、执行规则、完整图像池审计、长周期计划和 `research-state.yaml`。该动作只是数据质量审计，重复/近重复结果仅作为人工复核入口；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 51 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/audit_image_quality_outliers.py`，对 `full_algae_dewatermark_v1` 的 `2777` 张图像完成亮度、对比、饱和度、Laplacian 清晰度、Tenengrad 边缘能量、尺寸、长宽比和灰度熵的只读质量统计：全量 `2777/2777` 可统计，其中 `2773` 张由 OpenCV 解码，4 个 `GIF89a` 内容文件由 Pillow fallback 解码；按候选图像 `2%` 分位阈值生成 `507` 张质量异常人工复核候选。生成 `full_algae_dewatermark_v1_quality_audit.*` 和 `full_algae_dewatermark_v1_quality_audit_outliers.tsv`，并同步 README、状态总览、交接指南、执行规则、完整图像池审计、长周期计划和 `research-state.yaml`。该动作只是数据质量审计，质量异常结果不是自动排除依据；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 52 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_manual_review_sheets.py`，将完整图像池审计结果整理为人工复核入口：`decode_failures_review.tsv` 4 行、`exact_duplicates_review.tsv` 3 行、`near_duplicates_review.tsv` 30 行、`quality_outliers_review.tsv` 507 行，并汇总为 `all_manual_review_issues.tsv` 共 `544` 条 pending review issues；生成 `manual_review_index.{md,json}`。同步 README、状态总览、交接指南、执行规则、完整图像池审计、长周期计划和 `research-state.yaml`。这些 review sheets 只是人工复核入口，尚未形成转换、排除、去重、保留或 split leakage guard 决策；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 53 | 2026-05-25 | 数据 | 新增 `metrics/scripts/validate_fullpool_manual_review.py` 与 `docs/full_enhancement_dataset_manual_review_protocol_cn.md`，并对 `full_algae_dewatermark_v1_manual_review/all_manual_review_issues.tsv` 执行字段校验：当前 `overall_status=pending_manual_review`，总行数 `544`，pending `544`，reviewed `0`，needs_followup `0`，invalid `0`，P0/P1/P2 分别为 `7/134/403`。生成 `manual_review_validation_status_20260525.{md,json}` 与空的 `manual_review_invalid_rows_20260525.tsv`，并同步 README、状态总览、交接指南、执行规则、完整图像池审计、长周期计划和 `research-state.yaml`。该动作只是校验人工复核字段；当前仍不能派生清洗 manifest、转换/排除清单或 split leakage guard；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 54 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_p0_review_pack.py`，为完整图像池人工复核生成 P0 辅助包：覆盖 4 个 decode failures 和 3 个 exact duplicate groups，共 `7` 条 `machine_suggestion`，输出 `p0_review_recommendations.tsv`、`p0_contact_sheet.png`、`p0_review_pack_summary.{md,json}` 和逐项预览图。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。该辅助包只是机助复核建议，不是人工 `reviewer_decision`；当前 review sheets 仍为 `pending_manual_review`，不能据此派生 clean manifest、转换/排除清单或 split leakage guard；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 55 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/derive_fullpool_review_artifacts.py`，作为完整图像池人工复核完成后的唯一派生入口：未来只在 review sheet 无 invalid、无 pending、无 unresolved decision 时生成 `reviewed_cv2_clean_manifest.txt`，并派生转换、排除、去重、split leakage guard 和子集标签清单。当前运行生成 `metrics/manifests/full_algae_dewatermark_v1_manual_review/derived_review_artifacts/review_artifacts_status_20260525.{md,json}`，状态仍为 `pending_manual_review`、`can_generate_clean_manifest=False`，各派生表为空或仅含表头，未生成 clean manifest。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 56 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/audit_myedge_stage1_coupling_status.py`，从 `D:\Desktop\MyEdgeCodex` 只读同步 Stage1 -> MyEdge 168 张带 GT coupling 状态，生成 `docs/stage1_myedge_coupling_status_20260525_cn.{md,json}`。当前 MyEdge coupling manifest 为 `168` 行，GT 缺失 `0`，Stage1 六阶段缺失 `0`，MSFI raw-reference 和 DiffusionEdge raw-reference 缺失均为 `0`；P1 preflight 已具备 MSFI 50k 与 DiffusionEdge baseline 50k 的 config template 和 intake/sync 框架，但 P1 results intake 仍为 `not_started`、planned output roots 不存在、没有 P1 指标。同步 README、下游协议、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未运行训练、采样、`eval.py`、`show.py`、Stage1 2770 张 full run 或任何指标重算。 |
| 57 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_p1_review_pack.py`，为完整图像池人工复核生成 P1 辅助包：覆盖 `9` 条近重复强候选和 `125` 条质量异常关键候选，共 `134` 条 `machine_suggestion`，输出 `p1_review_recommendations.tsv`、`p1_near_duplicate_contact_sheet.png`、4 张 `p1_quality_contact_sheet_*.png`、`p1_review_pack_summary.{md,json}` 和 `134` 张逐项预览图。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。该辅助包只是机助复核建议，不是人工 `reviewer_decision`；质量异常建议优先用于退化子集/失败案例候选，不是自动排除依据；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 58 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_p2_review_pack.py`，为完整图像池人工复核生成 P2 辅助包：覆盖 `21` 条近重复候选和 `382` 条质量异常候选，共 `403` 条 `machine_suggestion`，输出 `p2_review_recommendations.tsv`、`p2_near_duplicate_contact_sheet.png`、10 张 `p2_quality_contact_sheet_*.png`、`p2_review_pack_summary.{md,json}` 和 `403` 张逐项预览图。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。该辅助包只是机助复核建议，不是人工 `reviewer_decision`；质量异常建议优先用于数据覆盖、退化分层、失败案例或有效难例候选，近重复建议优先用于 future split leakage guard；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 59 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_manual_review_dashboard.py`，将 P0/P1/P2 复核辅助包整合为统一人工复核入口：生成 `manual_review_dashboard_20260525.{md,json}` 和 `all_priority_review_queue.tsv`，队列共 `544` 行，按 P0/P1/P2 排序并为每行记录 `target_review_sheet`、预览图路径和 `machine_suggestion`。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。该 dashboard 和队列只是人工复核导航，不是 `reviewer_decision`、clean manifest 或 split leakage guard；当前人工复核仍为 `pending_manual_review`，未生成 clean manifest；本轮未启动 2770 张完整增强、未重跑正式评测、未覆盖正式 502/496 结果。 |
| 60 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_fullpool_manual_review_decision_template.py` 和 `metrics/scripts/apply_fullpool_manual_review_decisions.py` 的 dry-run 路径，为完整图像池人工复核补齐统一决策模板与安全回写入口：生成 `manual_review_decision_template.tsv` 共 `544` 行、`manual_review_decision_template_20260525.{md,json}`、`manual_review_decision_apply_report_20260525.{md,json}`、空的 apply plan 和 invalid report。当前模板未填写，dry-run 状态为 `no_decisions_to_apply`、invalid `0`、applied `False`，没有写回任何 review sheet。同步 README、状态总览、交接指南、执行规则、完整图像池审计、人工复核协议、长周期计划和 `research-state.yaml`。该动作不生成 clean manifest、不转换/删除原图、不运行 2770 张完整增强、不重跑正式评测、不覆盖正式 502/496 结果。 |
| 61 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_myedge_evidence_gap_dashboard.py`，只读汇总 Stage1Codex 与 `D:\Desktop\MyEdgeCodex` 的投稿证据缺口，生成 `docs/stage1_myedge_evidence_gap_dashboard_20260525_cn.{md,json}`。当前 dashboard 总状态为 `major_evidence_gaps_remain`：`complete=5`、`pending_manual_review=1`、`missing_blocked_by_manual_review=1`、`not_started=2`、`planned_only=4`、`ready_not_started=1`、`partial_complete=1`；Stage1 full-pool 人工复核仍有 `544` 条 pending、clean manifest 未生成、`full2770` 未开始；MyEdge formal MSFI 50k 结果为 ODS `0.783527` / OIS `0.794213` / AP `0.345899` / AC `0.796846`，但 Stage1 coupling P1 仍为 `not_started`，MSFI 消融/替换/退化子集/效率失败案例仍主要是 planned-only 合同。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未运行 Stage1 增强、MyEdge 训练/采样、`eval.py`、`show.py` 或任何指标重算。 |
| 62 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_myedge_next_gate_board.py`，生成 `docs/stage1_myedge_next_gate_board_20260525_cn.{md,json}`，把长期收口目标拆成 `10` 个执行门禁。当前状态为 `gated_long_cycle_incomplete`：G01 full-pool 人工复核仍 `blocked_pending_human_review` 且 pending `544`，G02 clean manifest `blocked_by_G01`，G03 Stage1 full2770 `not_started`，G04 no-GT proxy `complete`，G05 MyEdge P1 fixed-detector 为 `ready_after_explicit_approval`，G06/G07/G09 为 `planned_only`，G08 为 `planned_only_needs_labels`，G10 为 `active_guarded`。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。该门禁表只读现有状态文件，不运行 Stage1 增强、MyEdge 训练/采样、`eval.py`、`show.py` 或任何指标重算；planned contracts 和 machine suggestions 仍不能写成已完成结果。 |
| 63 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_myedge_p1_execution_readiness_pack.py`，生成 `docs/stage1_myedge_p1_execution_readiness_20260525_cn.{md,json}`，从 Stage1 侧只读固化 MyEdge P1 fixed-detector 执行准备状态。当前状态为 `ready_for_approval_not_executed`：7/7 前置条件满足，168 行 coupling manifest、Stage1 Final 源、GT、MSFI config template、DiffusionEdge baseline config template、baseline checkpoint 冻结记录和 planned output roots 均已核对；MyEdge 高风险确认短语记录为 `我确认执行高风险操作`；P1 pending intake 仍为 `not_started`。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未执行 staging、sampling、`eval.py`、`show.py`、report sync、Stage1 2770 张 full run 或任何指标重算；该准备包不能写成 P1 结果。 |
| 64 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_full2770_execution_readiness_pack.py`，生成 `docs/stage1_full2770_execution_readiness_20260525_cn.{md,json}`，从 Stage1 侧只读固化 2770 张 OpenCV 可读候选 full run 的执行准备状态。当前状态为 `candidate_full2770_ready_after_explicit_approval_clean_protocol_blocked`：8/8 前置条件满足，外部源目录可访问，cv2-readable candidate manifest raw/unique 均为 `2770`，decode audit 支持 2770 可读候选，smoke 与预算资产存在，run script 显式使用 `locked_full506_final_mainline.json` 和 candidate manifest，planned output root 不存在，intake 仍为 `not_started`，磁盘空间满足最低建议；但 reviewed clean full-pool protocol 仍被 `544` 条人工复核 pending 阻塞。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未运行 `main.py`、未创建 full2770 输出根目录、未生成增强图像、未重跑正式评测、未覆盖 502/496 正式结果；该准备包不能写成 full2770 完成结果或 reviewed clean protocol。 |
| 65 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_myedge_claim_evidence_ledger.py`，生成 `docs/stage1_myedge_claim_evidence_ledger_20260525_cn.{md,json}`，从 Stage1 侧只读固化 Stage1 / MyEdge 论文主张证据总账。当前总状态为 `claim_boundaries_locked_major_experiment_claims_pending`，共 `18` 条主张：`allowed_fact=3`、`allowed_with_boundary=7`、`readiness_only=2`、`planned_only_not_claimable=5`、`missing=1`；确认当前可写正式 502/496 口径、MyEdge MSFI mixed metric profile 和 full2770/P1 readiness，但 Stage1 下游 ODS/OIS/AP/AC 增益、MSFI 消融/替换优势、退化子集鲁棒、效率/PR/失败案例和两篇参考论文数据关系仍不能写成结果。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。本轮未运行 Stage1 增强、MyEdge 训练/采样、`eval.py`、`show.py`、图表生成或任何指标重算。 |
| 66 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_reference_dataset_relation_audit.py`，生成 `docs/reference_dataset_relation_audit_20260525_cn.{md,json}`，只读使用仓库文件和 Zotero 本地缓存核验两篇 Wu et al. 2026 同方向参考论文的数据描述：ESWA 边缘检测参考为 `676` 张、`36` 类、`473/203` train/test，并包含显微设备、倍率、明场、LabelMe 和双专家核验描述；EAAI 分割参考为 `1026` 张、`8:2` train/validation。同步更新 claim ledger：`C17` 由 missing 改为 `allowed_with_boundary`，当前总账计数为 `allowed_fact=3`、`allowed_with_boundary=8`、`readiness_only=2`、`planned_only_not_claimable=5`。同步 README、状态总览、交接指南、完整图像池审计、长周期计划和 `research-state.yaml`。该动作只是数据关系边界审计，仍不能声称 Stage1 `2777/2774/2770` 图像池与参考论文 `676/1026` 子集存在精确 overlap、同一 split、同一 GT 或同一采集协议；本轮未运行 Stage1 增强、MyEdge 训练/采样、`eval.py`、`show.py`、图表生成或任何指标重算。 |
| 67 | 2026-05-25 | 文档 | 新增并运行 `metrics/scripts/build_stage1_myedge_file_relation_audit.py`，生成 `docs/stage1_myedge_file_relation_audit_20260525_cn.{md,json}`，只读核对 Stage1 formal 502/496、MyEdge 168 coupling manifest、Stage1 formal outputs 与完整去水印图像池之间的文件级关系。当前 MyEdge 168 行 raw、GT、Stage1 六阶段输出均存在，`168/168` 按 stem 属于 `full502_clean_v1`，`166/168` 按 stem 属于 `compare9_complete496_v1`；但 MyEdge raw 与 Stage1 formal original 的 SHA256 相同数为 `0/168`，且与 `full_algae_dewatermark_v1` 的 `2774/2770` manifest 暂无文件名或 stem 直连。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。该动作不运行 Stage1 增强、不运行 MyEdge staging/sampling/`eval.py`/`show.py`、不生成指标、不生成图表；当前只能写 stem/path 对齐和 future fixed-detector 可执行条件，不能写同一原图文件、full-pool 精确重合或下游收益。 |
| 68 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_stage1_myedge_visual_relation_audit.py`，生成 `docs/stage1_myedge_visual_relation_audit_20260525_cn.{md,json}` 和 `metrics/manifests/stage1_myedge_visual_relation_candidates_20260525.tsv`，只读用 PIL、aHash、dHash 和 32x32 RMSE 对 MyEdge 168、Stage1 formal originals 和 full-pool 2774 做视觉候选关系审计。当前 MyEdge raw、Stage1 formal original、full-pool 2774 均可由 PIL 读取；MyEdge raw 与 Stage1 formal original 的 exact/strong 视觉候选为 `123/168`，MyEdge raw -> full-pool top-1 strong 候选为 `8/168`，Stage1 formal 502 -> full-pool top-1 exact/strong 候选为 `40/502`。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。该动作只是 provenance 复核辅助，不运行 Stage1 增强、不运行 MyEdge staging/sampling/`eval.py`/`show.py`、不生成边缘检测指标、不生成图表；visual hash 候选不能写成同一原图、同一 split、同一 GT 或参考论文数据集 overlap。 |
| 69 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/build_stage1_myedge_provenance_review_template.py` 与 `metrics/scripts/validate_stage1_myedge_provenance_review.py`，把 Stage1 / MyEdge / full-pool 视觉候选关系转成人工 provenance 复核入口，生成 `docs/stage1_myedge_provenance_review_protocol_cn.md`、`metrics/manifests/stage1_myedge_provenance_review/provenance_review_template.tsv`、`provenance_priority_review_queue.tsv`、`provenance_review_index_20260525.{md,json}`、`provenance_review_validation_status_20260525.{md,json}` 和空 invalid rows TSV。当前全量模板 `1510` 行，P0/P1 优先队列 `208` 行，`reviewer_decision` 已填写 `0` 行、pending `1510` 行、invalid `0` 行、paper-positive `0` 行。同步 README、状态总览、交接指南、长周期计划和 `research-state.yaml`。该动作只建立人工复核工作流，不运行 Stage1 增强、不运行 MyEdge staging/sampling/`eval.py`/`show.py`、不生成指标或图表；未填写并通过校验前，不能把任何候选写成同一原图、original-id 映射、split leakage guard 或参考论文数据集 overlap。 |
| 70 | 2026-05-25 | 数据 | 新增并运行 `metrics/scripts/derive_stage1_myedge_provenance_artifacts.py`，为人工 provenance 复核完成后的 confirmed original-id map、paper-usable provenance relations、negative/uncertain relations 和 split leakage guard candidates 建立唯一派生入口。当前生成 `metrics/manifests/stage1_myedge_provenance_review/derived_provenance_artifacts/provenance_artifacts_status_20260525.{md,json}` 以及 5 个空表头 TSV，状态为 `pending_manual_provenance_review`：pending `1510`、reviewed `0`、invalid `0`、positive relations `0`、paper-usable relations `0`、confirmed original IDs `0`、split leakage guard candidates `0`、`can_generate_final_provenance=false`。同步 README、状态总览、交接指南、provenance 协议、长周期计划和 `research-state.yaml`。该动作不推断人工结论，不运行 Stage1 增强、不运行 MyEdge staging/sampling/`eval.py`/`show.py`、不生成指标或图表；当前不能写任何 provenance 已确认结论。 |
| 71 | 2026-05-25 | 实验/文档 | 按长期目标执行并接收 Stage1 -> MyEdge P1 fixed-detector 诊断：在 `D:\Desktop\MyEdgeCodex` 中完成 168 张 Stage1 Final staging、MSFI 50k sampling、DiffusionEdge baseline 50k sampling、WSL `eval.py`/`show.py`、只读 intake 和 report-asset sync。MSFI P1 输出为 `output_test/stage1_coupling/msfi_50k/stage1_final_168_p1_20260524`，DiffusionEdge baseline P1 输出为 `output_test/stage1_coupling/diffusionedge_baseline_50k/stage1_final_168_p1_20260525`；两条 run 均有 `168` 个 PNG/MAT/NMS/white/overlay/error_map、`manifest.csv`、`run_report.md` 和日志。P1 mat-eval 结果为 Stage1 Final -> MSFI 50k ODS/OIS/AP/AC `0.588287/0.671357/0.263997/0.7403`，Stage1 Final -> DiffusionEdge baseline 50k `0.530094/0.567910/0.224073/0.7349`；这些结果低于 MyEdge raw-input formal anchors，当前只能写成 Stage1 Final fixed-detector 诊断/负向证据，不能写 Stage1 Final 已提升下游边缘检测。同步更新 Stage1 的 README、状态总览、交接指南、长周期计划、coupling status、evidence gap dashboard、next gate board、P1 readiness pack、claim evidence ledger 和 `research-state.yaml`，并同步 MyEdge 的 readme、project-state、交接/状态/写作证据文档和 research-log。Baseline 采样使用原始 DiffusionEdge 代码路径以匹配 checkpoint 键名，MyEdge wrapper key-mismatch 失败日志保留为诊断记录。本轮未运行 Stage1 2770 张 full run，未覆盖 Stage1 正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型，未生成投稿新图表。 |
| 72 | 2026-05-25 | 实验/文档 | 继续按长期目标完成 Stage1 -> MyEdge DiffusionEdge baseline stage-wise P2 诊断：在 `D:\Desktop\MyEdgeCodex` 中为 BPH、IMF1Ray、RGHS、CLAHE、Fused 五个阶段生成 168 张同 stem staging 输入，写入 baseline sampling configs，并用固定 DiffusionEdge baseline 50k checkpoint 完成 sampling、WSL `eval.py`/`show.py`、report-asset sync 和结果汇总。P2 汇总位于 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\stagewise_baseline_p2_results_20260525.{md,json,csv}`；Raw anchor ODS/OIS/AP/AC 为 `0.770521/0.779986/0.363065/0.7969`，BPH 为 `0.712919/0.722432/0.338546/0.7942`，IMF1Ray 为 `0.687163/0.722376/0.355401/0.7417`，RGHS 为 `0.612234/0.653750/0.286407/0.7519`，CLAHE 为 `0.584453/0.665213/0.265935/0.7534`，Fused 为 `0.629095/0.689792/0.302611/0.7539`，Final 为 `0.530094/0.567910/0.224073/0.7349`。结论边界：该矩阵只覆盖 DiffusionEdge baseline 单检测器，各 Stage1 阶段均低于 Raw anchor，当前只能写成负向/诊断性阶段归因，不能外推到 MSFI，也不能写成 Stage1 正向下游收益。同步更新 Stage1 的 README、状态总览、交接指南、长周期计划、evidence gap dashboard、next gate board、claim evidence ledger、`research-state.yaml`，并同步 MyEdge 的状态/写作证据文档和 `research-log.md`。本轮未运行 Stage1 2770 张 full run，未覆盖 Stage1 正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型，未生成投稿新图表。 |
| 73 | 2026-05-25 | 实验/文档 | 继续按长期目标完成 Stage1 -> MyEdge MSFI stage-wise P3 诊断：在 `D:\Desktop\MyEdgeCodex` 中为 BPH、IMF1Ray、RGHS、CLAHE、Fused 五个阶段生成固定 MSFI 50k sampling configs，复用 168 张同 stem staging 输入，完成 sampling、WSL `eval.py`/`show.py`、report-asset sync 和结果汇总。P3 汇总位于 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\stagewise_msfi_p3_results_20260525.{md,json,csv}`；Raw anchor ODS/OIS/AP/AC 为 `0.783527/0.794213/0.345899/0.796846`，BPH 为 `0.739237/0.752407/0.310610/0.7942`，IMF1Ray 为 `0.731747/0.754032/0.350926/0.7573`，RGHS 为 `0.671720/0.723929/0.296742/0.7619`，CLAHE 为 `0.639906/0.720816/0.279873/0.7750`，Fused 为 `0.669946/0.721055/0.291170/0.7683`，Final 为 `0.588287/0.671357/0.263997/0.7403`。结论边界：该矩阵只覆盖 MSFI 50k 单检测器，所有增强阶段 ODS/OIS/AC 均低于 Raw anchor，IMF1Ray AP 略高于 Raw 但不足以支持旧 Stage1 正向下游收益。同步更新 Stage1 的 README、状态总览、交接指南、长周期计划、evidence gap dashboard、next gate board、claim evidence ledger、`research-state.yaml`，并同步 MyEdge 的状态/写作证据文档和 `research-log.md`。本轮未运行 Stage1 2770 张 full run，未覆盖 Stage1 正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型，未生成投稿新图表。 |
| 74 | 2026-05-25 | 实验/代码/文档 | 针对 Stage1 -> MyEdge P1/P2/P3 负向诊断，新增不覆盖正式主线的 downstream-driven edge-safe enhancement P4：在 `main.py` 中加入 `save_intermediate_stages=false`、`final_refine.mode=original/bph/edge_preserve_blend` 和 edge-preserve blend 支持；新增 `experiments/downstream_driven_v1/` 三个配置和协议，先生成 168 张三变体，再为 selected `edge_preserve_raw_bph_moderate_v1` 生成 full502 输出，Final PNG/JPG 各 `502`，未启动 2770 full-pool。MyEdge 固定 MSFI 50k P4 结果为 historical Raw `0.783527/0.794213/0.345899/0.796846`，same-round original-control `0.783082/0.794168/0.337353/0.7972`，mild `0.782743/0.793599/0.345909/0.7957`，moderate `0.782999/0.794527/0.345952/0.7952`，legacy Final P1 `0.588287/0.671357/0.263997/0.7403`。结论边界：旧 Stage1 Final 确认显著降低固定检测器边缘指标；P4 moderate 基本消除该大幅损伤并接近 raw，但尚不能写稳定优于 raw 或 Stage1 正向收益已证明。同步更新 README、状态总览、交接指南、长周期计划、dashboard/gate/ledger、`research-state.yaml`，并同步 MyEdge 侧状态、写作证据和 `research-log.md`。 |
| 75 | 2026-05-25 | 实验/文档 | 继续按长期目标完成 downstream-driven edge-preserve P5C baseline-side 诊断：在 `D:\Desktop\MyEdgeCodex` 中为 original-control、raw-BPH mild、raw-BPH moderate 三个 P4 变体生成固定 DiffusionEdge baseline 50k sampling configs，完成 sampling、WSL `eval.py`/`show.py`、report-asset sync 和结果汇总。P5C 汇总位于 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\downstream_variant_baseline_p5c_results_20260525.{md,json,csv}`；historical raw DiffusionEdge anchor ODS/OIS/AP/AC 为 `0.770521/0.779986/0.363065/0.7969`，original-control 为 `0.771470/0.781490/0.362827/0.7934`，mild 为 `0.770699/0.782323/0.370985/0.7941`，moderate 为 `0.771168/0.782422/0.363047/0.7940`，legacy Stage1 Final 为 `0.530094/0.567910/0.224073/0.7349`。结论边界：P5C 说明 edge-preserve 变体不是 MSFI-only，且在第二固定检测器下也基本消除旧 Stage1 Final 的大幅下游损伤；mild 有 AP/OIS 正向信号，但 AC 仍低于 raw，不能写成稳定正向下游收益。同步更新 Stage1 的 README、状态总览、交接指南、长周期计划、dashboard/gate/ledger、`research-state.yaml`，并同步 MyEdge 侧状态、写作证据和 `research-log.md`。本轮未运行 Stage1 2770 张 full run，未覆盖 Stage1 正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型，未生成投稿新图表。 |
| 76 | 2026-05-25 | 文档/分析 | 同步并登记 MyEdge P6 结构/伪边 proxy 诊断：Stage1 侧三份 generator 已更新并重建 dashboard/gate/ledger，P6 状态为 complete；MyEdge 侧 P6 只读分析 P4/P5C 既有 MAT 与 GT，MSFI P4 moderate 相比 raw 在 F1、precision、false-edge ratio、components/kpx、endpoints/kpx 上有候选结构信号但 recall 略低，DiffusionEdge baseline P5C mild 为 mixed signal。同步 README、状态总览、交接指南、执行规则、长周期计划、`research-state.yaml` 和 MyEdge 状态/写作文档；本轮未运行 Stage1 2770 full run，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1`，未训练新模型，P6 不能替代 ODS/OIS/AP/AC 或写成稳定下游收益。 |
| 77 | 2026-05-25 | 文档/分析 | 继续推进 Stage1 -> MyEdge downstream-driven 证据链，新增 MyEdge P6B paired review：`D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\analyze_stage1_edge_structure_paired_review.py` 只读 P6 per-image CSV，生成 `downstream_variant_structure_p6_paired_review_20260525.{md,json,csv}`，按 stem 配对 raw anchor 与各变体并计算 mean directional improvement、bootstrap 95% CI 和 exact sign-test。关键结果：MSFI P4 moderate 相比 raw 的 paired proxy 为 precision `+0.00684`、false-edge `+0.00684`、F1 `+0.00146`、endpoints/kpx `+1.4171`、recall `-0.00357`；DiffusionEdge baseline P5C 侧仍 mixed。同步 Stage1 三份 generator 并重建 dashboard/gate/ledger，更新 README、状态总览、交接指南、长周期计划、`research-state.yaml`，并同步 MyEdge readme、状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。本轮未运行 Stage1 2770 full run，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1`，未训练新模型；P6B 只能作为结构 proxy 的 paired diagnostic，不能替代 ODS/OIS/AP/AC 或写成稳定下游收益。 |
| 78 | 2026-05-25 | 代码/实验/文档 | 继续推进 Stage1 -> MyEdge downstream-driven 证据链，新增 generic controls P7 preflight：在 `main.py` 中新增 `final_refine.mode=generic_luma_clahe/generic_luma_gamma` 两个非 BPH 亮度域对照，不改变正式 `locked_full506_final_mainline` 主线；新增 `experiments/downstream_driven_v1/configs/generic_luma_clahe_mild_v1.json` 与 `generic_luma_gamma_mild_v1.json`，并在 MyEdge 168 split 上生成两个变体的 Stage1 Final 输出，Final PNG/JPG 各 `168`、OpenCV decode failures `0`、非 Final 阶段目录为空。MyEdge 侧使用既有 prepare scripts 生成 MSFI 50k 与 DiffusionEdge baseline 50k 的 staging/config preflight，两个报告均 `row_count=168`、`missing_source=0`、`duplicate_target=0`、`staging_png_count=168`、planned output roots 不存在，状态为 `ready_for_sampling_not_evaluated`。同步 protocol、dashboard/gate/ledger、README、状态总览、交接指南、长期计划、`research-state.yaml`，并同步 MyEdge 状态文档和 `research-log.md`。边界：本轮未运行 MyEdge sampling/eval/show、未生成 ODS/OIS/AP/AC、AC 或结构 proxy，未启动 2770 full-pool，不能写 P7 已验证或 Stage1 稳定正向收益。 |
| 79 | 2026-05-25 | 实验/文档 | 继续推进 Stage1 -> MyEdge downstream-driven 证据链，完成 generic controls P7 fixed-detector 诊断：复用 `generic_luma_clahe_mild_v1` 与 `generic_luma_gamma_mild_v1` 两个 168 张非 BPH 亮度域对照，在 MyEdge 侧完成固定 MSFI 50k 与固定 DiffusionEdge baseline 50k 的 sampling、WSL `eval.py`/`show.py` 和 report-asset sync；多 run WSL 评测改用 `.sh` 脚本，避免 PowerShell 双引号提前展开 Bash 变量。P7 汇总位于 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\generic_control_p7_msfi_results_20260525.{md,json,csv}` 与 `generic_control_p7_baseline_results_20260525.{md,json,csv}`；MSFI 侧 Raw/CLAHE/gamma/legacy Final ODS/OIS/AP/AC 为 `0.783527/0.794213/0.345899/0.796846`、`0.781721/0.793016/0.345003/0.7934`、`0.782883/0.794223/0.345982/0.7952`、`0.588287/0.671357/0.263997/0.7403`；DiffusionEdge baseline 侧为 `0.770521/0.779986/0.363065/0.7969`、`0.769295/0.784093/0.366887/0.7915`、`0.771645/0.782024/0.371366/0.7936`、`0.530094/0.567910/0.224073/0.7349`。同步 Stage1 的 README、状态总览、交接指南、长期计划、downstream protocol、dashboard/gate/ledger 生成脚本和 `research-state.yaml`，并同步 MyEdge 状态/交接/写作证据文档和 `research-log.md`。边界：P7 只能写成 168 张 lightweight luminance-only control 诊断；gamma 在 baseline 侧有小幅正向信号、MSFI 侧基本贴近 raw，但缺 P7 结构 proxy、repeat/control 和更大口径验证，不能写成 Stage1 稳定提升下游。本轮未启动 2770 full-pool、未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录、未训练新模型、未生成投稿新图表。 |
| 80 | 2026-05-25 | 分析/文档 | 继续推进 Stage1 -> MyEdge downstream-driven 证据链，完成 generic controls P7 结构/伪边 proxy 与 paired review：新增 MyEdge 脚本 `D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\analyze_stage1_generic_control_p7_structure_metrics.py`，只读 P7 已有 MAT 与 GT，生成 `generic_control_p7_structure_metrics_20260525.{md,json,summary.csv,per_image.csv,delta_vs_raw.csv}`；扩展 `analyze_stage1_edge_structure_paired_review.py --mode p7`，生成 `generic_control_p7_structure_paired_review_20260525.{md,json,csv}`。关键结果：MSFI/gamma 相对 raw 的 F1/precision/recall/false-edge/components/endpoints delta 为 `+0.000692/+0.003978/-0.002338/-0.003978/-0.4290/-1.1210`，baseline/gamma 为 `-0.000688/-0.004253/+0.002480/+0.004253/-0.1828/+0.2251`；legacy Stage1 Final 在两检测器下继续表现为明显伪边和碎裂恶化。同步 Stage1 的 README、状态总览、交接指南、长期计划、downstream protocol、dashboard/gate/ledger 生成脚本和 `research-state.yaml`，并同步 MyEdge 状态/交接/写作证据文档和 `research-log.md`。边界：P7 结构 proxy 只支持 168 张 lightweight luminance-only control 诊断，MSFI/gamma 有轻微结构信号但 baseline/gamma 仍 mixed；仍不能写 Stage1 稳定提升下游，下一步是 P4/P5C/P7 repeat/control、退化/形态分层和更大口径前的人工复核。WSL 多 run 命令继续遵守 `.sh` 脚本规则，避免 PowerShell 双引号提前展开 Bash 变量。本轮未启动 2770 full-pool、未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录、未训练新模型、未生成投稿新图表。 |
| 81 | 2026-05-25 | 文档/准备 | 同步 Stage1 与 MyEdge 的 P4/P5C/P7 repeat-control P8 预执行状态，并强化 WSL 命令封装规则。MyEdge 侧新增 `docs/paper_assets/scripts/prepare_stage1_repeat_control_p8_assets.py`，生成 P8 run sheet、preflight `.md/.json`、10 个 `configs/stage1_coupling/*p8_repeat1_20260525.yaml` 和 `run_stage1_repeat_control_p8_eval_show_20260525.sh`；矩阵覆盖 5 个输入变体 x 2 个固定检测器，`row_count=168`，所有 planned runs 均 `ready_for_sampling_after_confirmation=True`，planned output roots 当前不存在或为空。Stage1 侧同步更新 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`，明确 P8 只是 readiness，不是已完成 repeat/control 结果；后续多 run WSL 评测必须使用 `.sh` 脚本或等价脚本文件，不能把 Bash 变量/数组/循环放进 PowerShell 双引号 `$cmd`。本轮未运行 Stage1 2770 full-pool，未运行 MyEdge P8 sampling、`eval.py` 或 `show.py`，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型，未生成投稿新图表。 |
| 82 | 2026-05-25 | 文档/准备 | 补齐 MyEdge P8 repeat-control 的结果接收链路，并同步 Stage1 侧状态。MyEdge 新增 `docs/paper_assets/scripts/sync_stage1_repeat_control_p8_results.py`，只读读取 P8 preflight 并检查 10 条 planned runs 的 output root、`png/mat/nms/mat-eval/nms-eval/logs` 与 report assets；当前生成 `stage1_repeat_control_p8_results_20260525.{md,json,csv}`，总状态为 `ready_not_executed`，10 条 P8 run 均 `not_started`。Stage1 同步 README、状态总览、交接指南、downstream protocol 和 `research-state.yaml`，明确 result intake 只是未执行状态证明，不是 P8 指标结果；执行 P8 前仍需高风险确认，执行后还需 intake/sync、结构 proxy 和 paired review。 |
| 83 | 2026-05-25 | 文档/分析 | 同步 MyEdge 退化子集与失败案例候选脚手架：MyEdge 新增并运行 `docs/paper_assets/scripts/build_stage1_degradation_failure_candidates.py`，只读 168 张 Stage1/MyEdge coupling manifest、raw 图、GT edge 和 P6/P7 per-image proxy，生成 `degradation_subset_candidates_20260525.{md,json,csv}` 与 `.failure_cases.csv`。当前状态为 `proxy_candidates_ready_pending_manual_review`，候选标签计数为 `low_contrast_boundary=42`、`blurred_contour=42`、`false_edge_background=42`、`thin_structure=42`、`overlap_clutter=49`、`unassigned_proxy=45`，success/failure 候选为 10 类各 top-20。同步 Stage1 的 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 的 readme、状态、交接、写作证据、`project-state.yaml` 和 `research-log.md`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；该候选集不能写成退化子集评测、人工标签或鲁棒性结果。 |
| 84 | 2026-05-25 | 文档/分析 | 继续推进退化子集与失败案例证据链，新增 MyEdge 人工复核工作流：`build_stage1_degradation_review_template.py` 生成 `degradation_review/degradation_failure_review_template.csv`、`degradation_failure_priority_queue.csv` 和 `degradation_failure_review_index_20260525.{md,json}`；`validate_stage1_degradation_review.py` 生成 validation status；`derive_stage1_degradation_review_artifacts.py` 生成 derived status 和空 reviewed artifacts。当前模板 `368` 行，其中 stratum candidate `168` 行、success/failure candidate `200` 行，P0/P1 priority queue `211` 行；validation 为 `pending_manual_review`、reviewed `0`、invalid `0`；derived reviewed stratification / failure panel / paper-positive artifacts 均为空。同步 Stage1 的 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 状态文档和 `research-log.md`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；该工作流不产生分层指标或论文失败案例结论。 |
| 85 | 2026-05-25 | 文档/分析 | 新增并运行 `metrics/scripts/build_stage1_downstream_edge_diagnostic.py`，只读汇总 `D:\Desktop\MyEdgeCodex` 已完成的 Stage1 -> MyEdge P2/P3/P4/P5C/P7 JSON/MD，生成 `docs/stage1_downstream_edge_negative_diagnostic_20260525_cn.{md,json}` 和 `docs/stage1_downstream_edge_metric_deltas_20260525.csv`。当前状态为 `negative_diagnostic_locked_repeat_control_pending`，stage-wise delta `12` 行、variant delta `20` 行，`legacy_stage1_final_hurts_downstream=true`，P8 状态为 `ready_not_executed`。结论：legacy Stage1 `Final` 在固定 DiffusionEdge baseline 50k 与固定 MSFI 50k 下均明显降低 ODS/OIS/AP/AC；DiffusionEdge baseline P2 中所有阶段均低于 Raw；MSFI P3 中所有阶段 ODS/OIS/AC 均低于 Raw，仅 IMF1Ray 的 AP 略高但整体仍为负向诊断；P4/P5C/P7 只能作为接近 raw 的 edge-safe/control 候选，不能写成稳定下游收益。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算，未覆盖正式 502/496 结果。 |
| 86 | 2026-05-25 | 文档/分析 | 同步 MyEdge PR / threshold trade-off 汇总状态：MyEdge 新增并运行 `D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\build_pr_threshold_tradeoff_report.py`，只读解析既有 `eval_bdry.txt` 与 P2/P3/P4/P5C/P7 结果 JSON，生成 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\pr_threshold_tradeoff_report_20260525.{md,json}` 和 `pr_threshold_tradeoff_rows_20260525.csv`。关键结果：MSFI raw 相比 DiffusionEdge baseline raw 的 ODS `+0.013006`、OIS `+0.014227`、AP `-0.017166`、precision@ODS `+0.008837`、recall@ODS `+0.015732`、AC `-0.000054`。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 侧 README、状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；该结果只能支持 operating-point gain with AP/ranking calibration trade-off，不能写成全面优于 baseline。 |
| 87 | 2026-05-25 | 文档/可视化 | 同步 MyEdge PR / threshold operating-point 可视化状态：MyEdge 新增并运行 `D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\draw_pr_threshold_operating_points.py`，只读 `pr_threshold_tradeoff_rows_20260525.csv`，生成 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\pr_threshold_visual_status_20260525.{md,json}`、`figures/pr_threshold_operating_points_20260525.{png,pdf,svg}` 和 `figures/pr_threshold_metric_delta_profile_20260525.{png,pdf,svg}`。两张 PNG 已做非空检查，尺寸分别为 `2017x1436` 和 `1940x1264`。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 侧 README、状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；由于现有 `eval_bdry.txt` 只有单行 summary，这些图只能称为 operating-point visuals / metric-delta profile，不能标成连续 PR curve。 |
| 88 | 2026-05-25 | 文档/可视化 | 同步 MyEdge failure-case panel 候选包：MyEdge 新增并运行 `D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\build_failure_case_panel_candidates.py`，只读 `degradation_subset_candidates_20260525.failure_cases.csv`、P6/P7 per-image proxy、既有 raw/GT/NMS/error-map 资产，生成 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\failure_case_panels_20260525\failure_case_panel_candidates_20260525.{md,json}`、`failure_case_panel_selection_20260525.csv`、40 个单图 panel 和 10 张 top-4 contact sheet。当前状态为 `failure_case_panel_candidates_ready_pending_manual_review`，10 类 success/failure category 每类 top-4，缺失资产 `0`。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 侧 README、状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；该包只是机器候选和人工复核入口，不能写成最终 paper-ready failure cases、退化鲁棒性结果或稳定 Stage1 下游收益。 |
| 89 | 2026-05-25 | 代码/实验/文档 | 继续推进 Stage1 -> MyEdge downstream-driven 分支，新增非正式 `edge_safe_gamma_bph_v1` 候选：在 `main.py` 中加入 `final.mode=edge_safe_gamma_bph`，该模式只对 raw Lab 亮度做轻量 gamma/contrast，并将 BPH Lab 色度小比例迁移到 raw，跳过 IMF1Ray/RGHS/CLAHE/Fusion/legacy Final；新增配置 `experiments/downstream_driven_v1/configs/edge_safe_gamma_bph_v1.json`。已先运行 2 张 MyEdge split smoke，再生成 168 张 MyEdge split Stage1 输出；168 张输出位于 `experiments/downstream_driven_v1/outputs/myedge168/edge_safe_gamma_bph_v1`，Final PNG/JPG 各 `168`、非 Final 阶段文件 `0`、Final PNG 解码 `168/168`，日志为 `experiments/downstream_driven_v1/logs/edge_safe_gamma_bph_v1_myedge168_stage1.log`，状态记录为 `edge_safe_gamma_bph_v1_smoke_status_20260525.{md,json}` 和 `edge_safe_gamma_bph_v1_myedge168_stage1_status_20260525.{md,json}`。同步 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未启动 2770 full-pool；该输出只能说明 Stage1 输入资产已就绪，不能写成下游收益。 |
| 90 | 2026-05-25 | 文档/准备 | 同步 MyEdge 侧 `edge_safe_gamma_bph_v1` P9 fixed-detector 预执行资产：MyEdge 新增 `docs/paper_assets/scripts/prepare_stage1_edge_safe_gamma_bph_p9_assets.py`，复用 Stage1 168 张 MyEdge split 输出，生成同 stem staging PNG `168` 张、固定 MSFI 50k 与固定 DiffusionEdge baseline 50k 两个 sampling config、`edge_safe_gamma_bph_p9_preflight_20260525.{md,json}` 和 `run_edge_safe_gamma_bph_p9_eval_show_20260525.sh`。两条 planned runs 均 `ready_for_sampling_after_confirmation=True`，planned output roots 均不存在。同步 Stage1 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 状态文档和 `research-log.md`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；P9 只是 readiness，不是 ODS/OIS/AP/AC 结果，执行前仍需明确高风险确认。 |
| 91 | 2026-05-25 | 文档/分析 | 新增并运行 `metrics/scripts/build_stage1_downstream_harm_degradation_diagnostic.py`，只读聚合 Stage1 已有 `stage1_downstream_edge_metric_deltas_20260525.csv`、MyEdge P6/P7 per-image 结构 proxy 和 `degradation_subset_candidates_20260525` 自动退化候选，生成 `docs/stage1_downstream_edge_harm_degradation_diagnostic_20260525_cn.{md,json}` 与 `docs/stage1_downstream_edge_harm_degradation_proxy_deltas_20260525.csv`。当前状态为 `readonly_harm_degradation_diagnostic_ready_auto_proxy_not_manual_strata`，stage-wise delta `12` 行、结构 proxy 聚合 `42` 行、failure category `10` 类。结论：legacy Stage1 `Final` 在 DiffusionEdge baseline 侧造成最大 ODS/OIS/AP/AC 下降；自动低对比、模糊、背景伪边、细结构、重叠/杂质候选中均出现 F1/precision 下降和 false-edge/endpoints 增加。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`。边界：本轮未运行 Stage1 增强、MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算；退化标签仍是自动 proxy、pending manual review，不能写成正式 per-stratum 结果。 |
| 92 | 2026-05-25 | 代码/实验/文档 | 继续推进 Stage1 -> MyEdge downstream-driven 分支，新增非正式 `boundary_aware_luma_bph_v1` 候选：在 `main.py` 中加入 `final.mode=boundary_aware_luma_bph`，该模式只用 raw+BPH，跳过 IMF1Ray/RGHS/CLAHE/Fusion/legacy Final，对 raw Lab 亮度做受限 gamma/contrast、低梯度背景轻微 bilateral smoothing、高梯度 masked unsharp 和最大亮度改变量限制，并只迁移少量 BPH 色度；新增配置 `experiments/downstream_driven_v1/configs/boundary_aware_luma_bph_v1.json`。已完成 2 张 smoke 与 168 张 MyEdge split Stage1 输出，Final PNG/JPG 各 `168`、非 Final 阶段文件 `0`、Final PNG 解码 `168/168`，状态记录为 `experiments/downstream_driven_v1/boundary_aware_luma_bph_v1_myedge168_stage1_status_20260525.{md,json}`。MyEdge 侧 P10 fixed-detector preflight 已生成 `boundary_aware_luma_bph_p10_preflight_20260525.{md,json}`、两条 sampling config、同 stem staging PNG `168` 张和 `run_boundary_aware_luma_bph_p10_eval_show_20260525.sh`，两条 planned runs 均 `ready_for_sampling_after_confirmation=True`，planned output roots 均不存在。同步 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或指标重算，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未启动 2770 full-pool；P10 只是 readiness，不是 ODS/OIS/AP/AC 或结构 proxy 结果，执行前仍需明确高风险确认。 |
| 93 | 2026-05-25 | 文档/准备 | 补齐 MyEdge 侧 `boundary_aware_luma_bph_v1` P10 result intake 并同步 Stage1 状态。MyEdge 新增 `docs/paper_assets/scripts/sync_stage1_boundary_aware_luma_bph_p10_results.py`，只读检查 P10 preflight 中两条 planned runs 的 output root、核心资产、日志和 report assets，默认运行生成 `boundary_aware_luma_bph_p10_results_20260525.{md,json,csv}`；当前总状态为 `ready_not_executed`，两条 run 均 `not_started`。同步 Stage1 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`，并同步 MyEdge readme、状态、交接、写作证据、`project-state.yaml` 和 `research-log.md`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练、Stage1 enhancement 或指标重算；P10 result intake 只是未执行状态证明和后续接收入口，不是下游指标结果。后续多 run WSL eval/show 必须使用生成的 `.sh`，不能把 Bash 变量/数组/循环放进 PowerShell 双引号 `$cmd` 再 `wsl bash -lc "$cmd"`。 |
| 94 | 2026-05-25 | 文档/准备 | 补齐 MyEdge 侧 `boundary_aware_luma_bph_v1` P10 structure proxy intake 并同步 Stage1 状态。MyEdge 新增 `docs/paper_assets/scripts/analyze_stage1_boundary_aware_luma_bph_p10_structure_metrics.py`，默认只读 P10 result intake 和既有 raw / legacy anchors；当前运行生成 `boundary_aware_luma_bph_p10_structure_metrics_20260525.{md,json}` 与 `.status.csv`，状态为 `blocked_by_missing_p10_core_results`，因为两条 P10 planned runs 均 `not_started`、MAT 计数均 `0`、无 mat-eval threshold。同步 Stage1 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`，并同步 MyEdge readme、状态、交接、写作证据、`project-state.yaml` 和 `research-log.md`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练、Stage1 enhancement 或指标重算；该 structure proxy intake 只是阻塞状态证明，不是结构指标或下游收益。 |
| 95 | 2026-05-25 | 文档/分析 | 新增并运行 Stage1 侧 MyEdge168 GT 边界代理预筛脚本 `metrics/scripts/build_stage1_myedge168_gt_edge_proxy_prescreen.py`。该脚本只读 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\stage1_myedge_168_coupling_manifest_20260524.csv`、raw 输入、GT edge、旧 Stage1 Final 和现有 downstream-driven 候选输出，用 Sobel/Otsu image-gradient proxy 与 GT edge 做 2 px 容差匹配，生成 `docs/stage1_myedge168_gt_edge_proxy_prescreen_20260525_cn.{md,json,summary.csv,delta_vs_raw.csv,per_image.csv}`。当前覆盖 168 张、9 个输入版本、1512 行逐图 proxy、失败 0。旧 Final 为 `proxy_negative_or_risky`：F1/precision/false-edge/endpoints 为 `0.409419/0.291554/0.708446/78.854652`，明显差于 raw `0.581331/0.476307/0.523693/56.100962`；P10 `boundary_aware_luma_bph_v1` 为 `proxy_edge_safe_candidate`，相对 raw dF1 `+0.001657`、dFalse-edge ratio `-0.001868`、dEndpoints/kpx `+0.363361`、mean luma delta `0.433829`。同步 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或正式 full502/full2770 增强；该预筛只支持 P10 fixed-detector 评测优先级，不能写成 ODS/OIS/AP/AC 或下游收益。 |
| 96 | 2026-05-25 | 代码/实验/文档 | 新增非正式 `skeleton_safe_luma_bph_v1` P11 候选：配置 `experiments/downstream_driven_v1/configs/skeleton_safe_luma_bph_v1.json` 复用 `boundary_aware_luma_bph` 代码路径，但关闭亮度增强和 unsharp，仅保留极小 BPH 色度迁移与低梯度平滑。已完成 2 张 smoke 与 168 张 MyEdge split Stage1 输出，状态记录为 `experiments/downstream_driven_v1/skeleton_safe_luma_bph_v1_smoke_status_20260525.{md,json}` 和 `experiments/downstream_driven_v1/skeleton_safe_luma_bph_v1_myedge168_stage1_status_20260525.{md,json}`；168 张输出 Final PNG/JPG 各 `168`、非 Final 阶段文件 `0`、Final PNG 解码 `168/168`。更新并重跑 `metrics/scripts/build_stage1_myedge168_gt_edge_proxy_prescreen.py`，当前覆盖 10 个输入版本、1680 行逐图 proxy、失败 0；P11 为当前第一项 `proxy_positive_candidate`，相对 raw dF1 `+0.001075`、dFalse-edge ratio `-0.001783`、dEndpoints/kpx `-1.302699`、mean luma delta `0.557185`。同步 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练或正式 full502/full2770 增强；P11 仍不是 ODS/OIS/AP/AC 或下游收益结果，下一步应先生成 MyEdge fixed-detector preflight，再经高风险确认执行 fixed-detector。 |
| 97 | 2026-05-25 | 文档/准备 | 同步 MyEdge 侧 `skeleton_safe_luma_bph_v1` P11 fixed-detector 预执行链路：MyEdge 复用已泛化的 `docs/paper_assets/scripts/prepare_stage1_edge_safe_gamma_bph_p9_assets.py`，生成 P11 同 stem staging PNG `168` 张、固定 MSFI 50k 与固定 DiffusionEdge baseline 50k 两个 sampling config、`skeleton_safe_luma_bph_p11_preflight_20260525.{md,json}` 和 `run_skeleton_safe_luma_bph_p11_eval_show_20260525.sh`；泛化后的 `sync_stage1_repeat_control_p8_results.py` 生成 `skeleton_safe_luma_bph_p11_results_20260525.{md,json,csv}`，当前 `ready_not_executed`、两条 run 均 `not_started`；新增 `analyze_stage1_single_variant_structure_metrics.py` 生成 `skeleton_safe_luma_bph_p11_structure_metrics_20260525.{md,json}` 和 `.status.csv`，当前 `blocked_by_missing_core_results`。同步 Stage1 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`，并同步 MyEdge 状态文档、根入口 WSL 规则和 `research-log.md`。边界：本轮未运行 MyEdge sampling、WSL `eval.py`、WSL `show.py`、训练、Stage1 enhancement 或指标重算；P11 仍是 readiness，不是 ODS/OIS/AP/AC 或结构指标结果。后续 WSL 多 run eval/show 必须使用 `.sh`，不能把 Bash 变量/数组/循环放入 PowerShell 双引号 `$cmd`。 |
| 98 | 2026-05-25 | 文档/准备 | 新增 MyEdge P11 高风险执行合同 `D:\Desktop\MyEdgeCodex\docs\research_contracts\stage1_skeleton_safe_luma_bph_p11_run_sheet_v1.md`，把 `skeleton_safe_luma_bph_v1` 后续执行收束为一个固定入口：先做只读 preflight recheck，再按 Windows PowerShell 分别运行 MSFI 50k sampling 与 DiffusionEdge baseline 50k sampling，随后只用 `run_skeleton_safe_luma_bph_p11_eval_show_20260525.sh` 进行 WSL eval/show，最后执行 result intake `--write-assets` 和 structure proxy。同步 README、状态总览、交接指南、长期计划和 `research-state.yaml`，并同步 MyEdge 状态文档和 `research-log.md`。边界：本轮未运行 P11 sampling、WSL `eval.py`、WSL `show.py`、result intake `--write-assets`、structure metrics、训练、Stage1 enhancement 或指标重算；P11 仍为 `ready_not_executed` / `blocked_by_missing_core_results`，不是下游收益证据。 |
| 99 | 2026-05-25 | 实验/文档 | 完成 MyEdge P11 `skeleton_safe_luma_bph_v1` fixed-detector 执行闭环：在 `D:\Desktop\MyEdgeCodex` 中按 P11 run sheet 完成固定 MSFI 50k 和固定 DiffusionEdge baseline 50k sampling，两条 run 均输出 168 PNG / 168 MAT；随后只用 `run_skeleton_safe_luma_bph_p11_eval_show_20260525.sh` 完成 WSL `eval.py` / `show.py`，避免 PowerShell 双引号提前展开 Bash 变量；运行 result intake `--write-assets`、结构/伪边 proxy 和 downstream gate。P11 结果入口为 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\skeleton_safe_luma_bph_p11_results_20260525.md`，MSFI ODS/OIS/AP/AC 为 `0.784173/0.795553/0.346803/0.7947`，DiffusionEdge baseline 为 `0.773642/0.782955/0.374479/0.7934`；结构 proxy 入口为 `skeleton_safe_luma_bph_p11_structure_metrics_20260525.md`；gate 入口为 `skeleton_safe_luma_bph_p11_downstream_gate_20260525.md`，结论为 `candidate_rescues_legacy_but_not_near_raw`。同步 README、状态总览、交接指南、长期计划、downstream protocol 和 `research-state.yaml`，并同步 MyEdge 侧 readme、状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。边界：P11 明显救回 legacy Final 下游损伤，MSFI 侧 raw-near 且结构 non-worse，但 DiffusionEdge baseline 侧仍有 AC/endpoints trade-off，不能写成 Stage1 稳定提升下游；本轮未运行 Stage1 2770 full-pool，未覆盖正式 `full502_clean_v1` / `compare9_complete496_v1` 结果目录，未训练新模型。 |
| 100 | 2026-05-25 | 代码/实验/文档 | 新增并执行 P12 `degradation_aware_pyramid_frequency_v1` 非正式多模块 downstream-driven 候选：`main.py` 新增 `final.mode=degradation_aware_pyramid_frequency_bph`，配置为 `experiments/downstream_driven_v1/configs/degradation_aware_pyramid_frequency_v1.json`；完成 2 张 smoke、168 张 MyEdge split Stage1 输出和 502 张 `full502_clean_v1` 输出。MyEdge 侧新增通用预检脚本 `D:\Desktop\MyEdgeCodex\docs\paper_assets\scripts\prepare_stage1_single_candidate_assets.py`，生成 P12 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P12 MSFI ODS/OIS/AP/AC 为 `0.786357/0.798143/0.344136/0.7925`，DiffusionEdge baseline 为 `0.774611/0.788854/0.376262/0.7886`；结构 proxy 对两个 detector 均 non-worse，但 AP/AC trade-off 仍存在，gate 为 `candidate_rescues_legacy_but_not_near_raw`。P12 full502 增强指标为 EME `3.1175`、Contrast `38.9595`、AvgGra `3.3848`、MS_SSIM `0.9941`、PSNR `41.3295`、UCIQE `2.2684`、UIQM `8.4089`；compare496 口径为 EME `3.0130`、Contrast `37.4205`、AvgGra `3.3176`、MS_SSIM `0.9941`、PSNR `41.3560`、UCIQE `2.2933`、UIQM `8.2337`。同步 downstream protocol、状态总览、交接指南和 `research-state.yaml`，并在 MyEdge 同步 P12 状态与日志。边界：旧 Stage1 Final 确实导致固定 detector 下游指标下降；P12 能救回崩塌但不是强通过候选，且增强强度明显弱于 FormalFinal；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 101 | 2026-05-25 | 代码/实验/文档 | 新增并执行 P13 `calibrated_pyramid_frequency_v1` 非正式校准候选：`main.py` 为 P12 的 `degradation_aware_pyramid_frequency_bph` 路径新增 raw-edge rescue 与 global raw blend 参数，配置为 `experiments/downstream_driven_v1/configs/calibrated_pyramid_frequency_v1.json`；完成 168 张 MyEdge split Stage1 输出、固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy、downstream gate，并补跑 `full502_clean_v1` 与 `compare9_complete496_v1` 增强指标。P13 MSFI ODS/OIS/AP/AC 为 `0.784769/0.796472/0.345621/0.7932`，DiffusionEdge baseline 为 `0.774132/0.786695/0.369667/0.7917`；gate 仍为 `candidate_rescues_legacy_but_not_near_raw`。P13 full502 增强指标为 EME `2.8379`、Contrast `31.6862`、AvgGra `3.0918`、MS_SSIM `0.9974`、PSNR `46.7696`、UCIQE `2.2573`、UIQM `7.8690`；compare496 为 EME `2.7324`、Contrast `30.2551`、AvgGra `3.0258`、MS_SSIM `0.9974`、PSNR `46.7865`、UCIQE `2.2820`、UIQM `7.6897`。边界：P13 救回 legacy Final 崩塌但不是强通过；相对 P12 只回收部分 AP/AC，却牺牲 P12 的 ODS/OIS 与 baseline AP，增强强度更弱；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 102 | 2026-05-25 | 代码/实验/文档 | 新增并执行 P14 `weak_boundary_pyramid_fusion_v1` 非正式弱边界局部融合候选：`main.py` 新增 `final.mode=weak_boundary_pyramid_fusion_bph`，以 skeleton-safe raw/BPH base 为底，仅在低对比弱边界区域注入 degradation-aware pyramid-frequency candidate，并加入伪边抑制、raw-edge rescue、global raw blend 和亮度改变量限制；配置为 `experiments/downstream_driven_v1/configs/weak_boundary_pyramid_fusion_v1.json`。完成 2 张 smoke、168 张 MyEdge split Stage1 输出、502 张 `full502_clean_v1` 输出、固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy、downstream gate，并补跑 `full502_clean_v1` 与 `compare9_complete496_v1` 增强指标。P14 MSFI ODS/OIS/AP/AC 为 `0.784373/0.795164/0.346405/0.7940`，DiffusionEdge baseline 为 `0.773454/0.784037/0.372930/0.7935`；结构 proxy vs raw 为 MSFI dF1 `+0.0017`、dFalse-edge `-0.0151`、dEndpoints/kpx `-1.4952`，baseline dF1 `+0.0013`、dFalse-edge `+0.0034`、dEndpoints/kpx `-0.1187`；gate 仍为 `candidate_rescues_legacy_but_not_near_raw`。P14 full502 增强指标为 EME `2.6005`、Contrast `26.9240`、AvgGra `2.7764`、MS_SSIM `0.9981`、PSNR `47.6219`、UCIQE `2.2432`、UIQM `7.3917`；compare496 为 EME `2.4955`、Contrast `25.5663`、AvgGra `2.7122`、MS_SSIM `0.9981`、PSNR `47.6324`、UCIQE `2.2674`、UIQM `7.2119`。边界：P14 救回 legacy Final 崩塌但不是强通过；MSFI 侧 raw-near 且结构 non-worse，baseline 侧 AC 与 false-edge 仍 mixed；P14 比 P13/P12 更保守，不能作为正式增强主线或稳定下游收益证据；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 103 | 2026-05-26 | 实验/文档/规则 | 完成并同步 Stage1 -> MyEdge P4/P5C/P7 repeat-control P8 阶段性收口：MyEdge 侧 5 个输入变体 x 2 个固定检测器共 10 条 run 均完成 sampling、WSL `.sh` eval/show、result intake `--write-assets`、结构 proxy 和专用 gate；P8 结果入口为 `D:\Desktop\MyEdgeCodex\docs\paper_assets\stage1_coupling\stage1_repeat_control_p8_results_20260525.md`，结构入口为 `stage1_repeat_control_p8_structure_metrics_20260525.md`，gate 入口为 `stage1_repeat_control_p8_gate_20260525.md`。Gate 结论为 `repeat_stable_but_control_competitive_no_stage1_specific_upgrade`：主要 edge-preserve/gamma 信号具备 repeat-stable near-raw rescue，但 generic gamma control 竞争力强且 baseline-side structure 仍 mixed，不能写成 Stage1-specific 稳定正向下游收益。重跑并更新 `metrics/scripts/build_stage1_downstream_edge_diagnostic.py` 及 `docs/stage1_downstream_edge_negative_diagnostic_20260525_cn.{md,json}`，当前总状态为 `negative_diagnostic_locked_p8_completed_control_competitive`。同时复核 WSL 命令错误根因：不是缺少 MyEdge WSL 文档，而是复杂 Bash 被放入 PowerShell 双引号 `$cmd` 后，`$run`、`$root`、`$PY`、`${run}` 被 PowerShell 提前展开；Stage1/MyEdge 入口规则已保留硬规则，后续简单 WSL 命令用单引号，多 run/数组/循环必须写 `.sh` 后 `wsl bash script.sh`。本轮未进入 2770 full-pool，未覆盖正式 Stage1 主线结果，未训练新模型。 |
| 104 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P15 `structure_guarded_weak_boundary_v1` 非正式结构守卫弱边界候选：新增配置 `experiments/downstream_driven_v1/configs/structure_guarded_weak_boundary_v1.json`，复用既有 `final.mode=weak_boundary_pyramid_fusion_bph` 代码路径，在 P14 基础上提高背景/伪边守卫和 raw 强边界 rescue；完成 2 张 smoke、168 张 MyEdge split Stage1 输出、502 张 `full502_clean_v1` 输出。MyEdge 侧生成 P15 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P15 MSFI ODS/OIS/AP/AC 为 `0.784104/0.795471/0.347023/0.7951`，DiffusionEdge baseline 为 `0.773201/0.782997/0.365411/0.7953`；结构 proxy vs raw 为 MSFI dF1 `+0.0033`、dFalse-edge `-0.0123`、dEndpoints/kpx `-1.6983`，baseline dF1 `+0.0013`、dFalse-edge `+0.0017`、dEndpoints/kpx `+0.1126`；gate 为 `candidate_metric_near_raw_structure_mixed`。P15 full502 增强指标为 EME `2.5211`、Contrast `25.4229`、AvgGra `2.6947`、MS_SSIM `0.9982`、PSNR `47.5609`、UCIQE `2.2555`、UIQM `7.2461`；compare496 为 EME `2.4151`、Contrast `24.1041`、AvgGra `2.6313`、MS_SSIM `0.9981`、PSNR `47.5692`、UCIQE `2.2799`、UIQM `7.0637`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 状态/交接/写作证据文档、`project-state.yaml` 和 `research-log.md`。边界：P15 指标 near-raw、MSFI 侧结构 non-worse，但 baseline-side false-edge/endpoints 仍 mixed；不能写成 Stage1 稳定下游收益，不能作为正式增强主线，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 105 | 2026-05-26 | 文档/方法设计 | 只读接收 `docs/downstream_driven_v1_method_design_inputs_20260526.md` 中三份 Web AI 回答，并新增 `docs/downstream_driven_v1_method_design_synthesis_20260526_cn.md` 作为后续 `downstream_driven_v1` 代码修改和实验的设计约束。该综合去重候选论文/项目/模块，用 Crossref 核验关键 DOI、arXiv export API 核验 arXiv ID，并用 GitHub HEAD 检查关键代码仓库可访问性；修正 EAAI 2026 HAB segmentation anchor 的 DOI 为 `10.1016/j.engappai.2026.114948`；将 Wu/Gengkun Wu 相关 HAB 论文单独标为 nearest-neighbor / overlap-risk anchor，不作为第一轮 candidate 主来源；把方法整理为 A/B/C 类，提取颜色补偿、白平衡、Retinex、CLAHE、Gamma/contrast、多尺度/金字塔融合、小波/频域、edge-aware smoothing/sharpening、false-edge suppression 和 parameter search 等可迁移模块；设计并排序 C01-C05 候选 pipeline，第一轮只建议推进 `c01_microstructure_csp_v1`，采用单候选、单 gate、单 run 目录计划。同步 README、状态总览、交接指南和 `research-state.yaml`。边界：本轮未修改增强代码、未运行 Stage1/MyEdge 实验、未生成新指标、未进入 2770 full-pool；C01 只是下一步执行计划，不是已实现或已通过 gate 的结果。 |
| 106 | 2026-05-26 | 代码/实验/文档 | 新增并执行 C01/P16 `c01_microstructure_csp_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=microstructure_csp_bph`，配置为 `experiments/downstream_driven_v1/configs/c01_microstructure_csp_v1.json`；完成 2 张 smoke、168 张 MyEdge split Stage1 输出和 502 张 `full502_clean_v1` 输出，更新 MyEdge168 GT edge proxy prescreen 到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_20260526_cn.*`。MyEdge 侧生成 P16 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P16 MSFI ODS/OIS/AP/AC 为 `0.783352/0.795226/0.346031/0.7948`，DiffusionEdge baseline 为 `0.771525/0.783504/0.370706/0.7941`；结构 proxy vs raw 为 MSFI dF1 `+0.0010`、dFalse-edge `-0.0101`、dEndpoints/kpx `-1.2432`，baseline dF1 `-0.0016`、dFalse-edge `+0.0071`、dEndpoints/kpx `+0.0941`；gate 为 `candidate_metric_near_raw_structure_mixed`。P16 full502 增强指标为 EME `2.6576`、Contrast `28.1097`、AvgGra `2.9418`、MS_SSIM `0.9987`、PSNR `49.4034`、UCIQE `2.2439`、UIQM `7.5138`；compare496 为 EME `2.5661`、Contrast `26.6626`、AvgGra `2.8745`、MS_SSIM `0.9987`、PSNR `49.4311`、UCIQE `2.2685`、UIQM `7.3591`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`，并同步 MyEdge 侧状态与日志。边界：P16 救回 legacy Final 下游损伤且指标接近 raw，但 baseline-side structure proxy 仍 mixed；不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 107 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P17 `topology_guarded_microfusion_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=topology_guarded_microfusion_bph`，配置为 `experiments/downstream_driven_v1/configs/topology_guarded_microfusion_v1.json`；完成 2 张 smoke、168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p17_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出。MyEdge 侧生成 P17 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P17 MSFI ODS/OIS/AP/AC 为 `0.784021/0.795041/0.346753/0.7945`，DiffusionEdge baseline 为 `0.771878/0.781854/0.364359/0.7958`；结构 proxy vs raw 为 MSFI dF1 `+0.0021`、dFalse-edge `-0.0165`、dEndpoints/kpx `-1.2064`，baseline dF1 `+0.0005`、dFalse-edge `+0.0031`、dEndpoints/kpx `+0.0958`；gate 为 `candidate_metric_near_raw_structure_mixed`。P17 full502 增强指标为 EME `2.5393`、Contrast `25.6767`、AvgGra `2.7598`、MS_SSIM `0.9989`、PSNR `47.7074`、UCIQE `2.2295`、UIQM `7.3876`；compare496 为 EME `2.4391`、Contrast `24.3284`、AvgGra `2.6952`、MS_SSIM `0.9989`、PSNR `47.7089`、UCIQE `2.2538`、UIQM `7.2173`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`，并同步 MyEdge 侧状态与日志。边界：P17 救回 legacy Final 下游损伤且指标接近 raw，MSFI 侧 structure non-worse，baseline F1 略升但 false-edge/endpoints 仍 mixed；不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 108 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P18 `topology_pruned_microfusion_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=topology_pruned_microfusion_bph`，配置为 `experiments/downstream_driven_v1/configs/topology_pruned_microfusion_v1.json`；完成 2 张 smoke、168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p18_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出。MyEdge 侧生成 P18 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P18 MSFI ODS/OIS/AP/AC 为 `0.783694/0.794872/0.346526/0.7946`，DiffusionEdge baseline 为 `0.772249/0.781858/0.363790/0.7946`；结构 proxy vs raw 为 MSFI dF1 `+0.0014`、dFalse-edge `-0.0153`、dEndpoints/kpx `-1.2397`，baseline dF1 `-0.0006`、dFalse-edge `+0.0041`、dEndpoints/kpx `+0.4725`；gate 为 `candidate_metric_near_raw_structure_mixed`。P18 full502 增强指标为 EME `2.5688`、Contrast `26.2070`、AvgGra `2.7968`、MS_SSIM `0.9988`、PSNR `48.3252`、UCIQE `2.2321`、UIQM `7.4075`；compare496 为 EME `2.4731`、Contrast `24.8484`、AvgGra `2.7316`、MS_SSIM `0.9988`、PSNR `48.3345`、UCIQE `2.2563`、UIQM `7.2484`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`，并同步 MyEdge 侧状态与日志。边界：P18 救回 legacy Final 下游损伤且指标接近 raw，MSFI 侧 structure non-worse，但 baseline 侧 F1/false-edge/endpoints 仍 mixed；不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 109 | 2026-05-26 | 实验/文档 | 新增并执行 P19 `baseline_stabilized_microfusion_v1` 非正式 downstream-driven 候选：新增配置 `experiments/downstream_driven_v1/configs/baseline_stabilized_microfusion_v1.json`，复用 `topology_pruned_microfusion_bph` 但放宽 raw-edge support、降低剪枝强度并收紧候选梯度/方差守卫；完成 2 张 smoke、168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p19_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出。MyEdge 侧生成 P19 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P19 MSFI ODS/OIS/AP/AC 为 `0.783842/0.795042/0.346413/0.7959`，DiffusionEdge baseline 为 `0.772365/0.782127/0.364005/0.7941`；结构 proxy vs raw 为 MSFI dF1 `+0.0015`、dFalse-edge `-0.0158`、dEndpoints/kpx `-1.4579`，baseline dF1 `-0.0002`、dFalse-edge `+0.0037`、dEndpoints/kpx `+0.1113`；gate 为 `candidate_metric_near_raw_structure_mixed`。P19 full502 增强指标为 EME `2.5649`、Contrast `26.1325`、AvgGra `2.7940`、MS_SSIM `0.9989`、PSNR `48.0644`、UCIQE `2.2332`、UIQM `7.4023`；compare496 为 EME `2.4682`、Contrast `24.7803`、AvgGra `2.7290`、MS_SSIM `0.9989`、PSNR `48.0723`、UCIQE `2.2573`、UIQM `7.2415`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`。边界：P19 救回 legacy Final 下游损伤且指标接近 raw，MSFI 侧 structure non-worse；baseline 侧仍 mixed，但相对 P18 endpoint 损伤明显缩小；不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 110 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P20 `endpoint_stabilized_weak_boundary_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=endpoint_stabilized_weak_boundary_bph`，配置为 `experiments/downstream_driven_v1/configs/endpoint_stabilized_weak_boundary_v1.json`；该模式在弱边界候选后对新增离 raw 支撑的小组件、细线状毛刺和高风险背景梯度做 raw/base pullback。完成 2 张 smoke、168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p20_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出。MyEdge 侧生成 P20 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P20 MSFI ODS/OIS/AP/AC 为 `0.783300/0.794816/0.346679/0.7945`，DiffusionEdge baseline 为 `0.772955/0.782373/0.364863/0.7950`；结构 proxy vs raw 为 MSFI dF1 `+0.0023`、dFalse-edge `-0.0032`、dEndpoints/kpx `-0.6257`，baseline dF1 `+0.0004`、dFalse-edge `+0.0034`、dEndpoints/kpx `+0.3098`；gate 为 `candidate_metric_near_raw_structure_mixed`。P20 full502 增强指标为 EME `2.5018`、Contrast `25.3270`、AvgGra `2.6663`、MS_SSIM `0.9986`、PSNR `47.0734`、UCIQE `2.2316`、UIQM `7.2074`；compare496 为 EME `2.3983`、Contrast `24.0276`、AvgGra `2.6034`、MS_SSIM `0.9986`、PSNR `47.0799`、UCIQE `2.2559`、UIQM `7.0297`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`，并同步 MyEdge 状态文档和日志。边界：P20 救回 legacy Final 下游损伤且指标接近 raw，MSFI 侧 structure non-worse；baseline 侧 false-edge/endpoints 仍 mixed，且 endpoint delta 差于 P19，因此不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 111 | 2026-05-26 | 文档/方法设计 | 按用户要求暂停后续实验，二次只读接收并复核 `docs/downstream_driven_v1_method_design_inputs_20260526.md` 与既有综合稿 `docs/downstream_driven_v1_method_design_synthesis_20260526_cn.md`。确认综合稿已覆盖去重、DOI/arXiv/GitHub/official link 核验边界、Wu/Gengkun Wu anchor 标注、A/B/C 分类、可迁移模块、C01-C05 candidate pipeline、排序、第一轮单候选执行计划、168/502/496/2770 口径；补充综合稿第 10 节“二次只读复核记录”。本轮仅做文档复核和日志同步：没有修改增强代码，没有运行 Stage1/MyEdge 实验，没有生成新指标，没有进入 2770 full-pool；`c01_microstructure_csp_v1` 在该文档语境中仍只是方法设计约束，后续真实结论必须以本地 manifest、结果表、run report、`eval_bdry.txt`、`show.log` 和结构 proxy 文件为准。 |
| 112 | 2026-05-26 | 实验/文档 | 新增并执行 P21 `balanced_weak_boundary_pyramid_fusion_v1` 非正式 downstream-driven 候选：新增配置 `experiments/downstream_driven_v1/configs/balanced_weak_boundary_pyramid_fusion_v1.json`，复用 `final.mode=weak_boundary_pyramid_fusion_bph`，在 P14 弱边界局部融合信号与 P15/P20 raw-near 守卫之间做平衡。完成 2 张 smoke、168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p21_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P21 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P21 MSFI ODS/OIS/AP/AC 为 `0.784437/0.795026/0.346698/0.7946`，DiffusionEdge baseline 为 `0.773458/0.783512/0.364882/0.7927`；结构 proxy vs raw 为 MSFI dF1 `+0.0033`、dFalse-edge `-0.0118`、dEndpoints/kpx `-1.4988`，baseline dF1 `+0.0016`、dFalse-edge `+0.0019`、dEndpoints/kpx `-0.3079`；gate 为 `candidate_rescues_legacy_but_not_near_raw`。P21 full502 增强指标为 EME `2.5482`、Contrast `25.8441`、AvgGra `2.7231`、MS_SSIM `0.9983`、PSNR `47.6669`、UCIQE `2.2421`、UIQM `7.2925`；compare496 为 EME `2.4420`、Contrast `24.5117`、AvgGra `2.6595`、MS_SSIM `0.9982`、PSNR `47.6755`、UCIQE `2.2663`、UIQM `7.1100`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`。边界：P21 救回 legacy Final 下游损伤且改善 baseline endpoints，但 baseline AC 和 false-edge ratio 仍 worse than raw，不能写成 Stage1 稳定下游收益；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 113 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P22 `ac_guarded_weak_boundary_fusion_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=ac_guarded_weak_boundary_bph`，配置为 `experiments/downstream_driven_v1/configs/ac_guarded_weak_boundary_fusion_v1.json`，在 P21-like 弱边界候选外加入 off-support pseudo-edge pruning、support-gradient floor、raw/base pullback 和亮度改变量 fallback。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p22_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P22 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P22 MSFI ODS/OIS/AP/AC 为 `0.785414/0.796161/0.346719/0.7946`，DiffusionEdge baseline 为 `0.773537/0.783375/0.365145/0.7936`；结构 proxy vs raw 为 MSFI dF1 `+0.002431`、dFalse-edge `-0.011902`、dEndpoints/kpx `-1.341618`，baseline dF1 `+0.000807`、dFalse-edge `+0.002607`、dEndpoints/kpx `+0.292560`；gate 为 `candidate_rescues_legacy_but_not_near_raw`。P22 full502 增强指标为 EME `2.5276`、Contrast `25.6124`、AvgGra `2.7088`、MS_SSIM `0.9982`、PSNR `47.5735`、UCIQE `2.2331`、UIQM `7.2513`；compare496 为 EME `2.4225`、Contrast `24.2955`、AvgGra `2.6455`、MS_SSIM `0.9982`、PSNR `47.5803`、UCIQE `2.2574`、UIQM `7.0715`。同步 downstream protocol、README、状态总览、交接指南和 `research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P22 救回 legacy Final 下游损伤并在两检测器 ODS/OIS/AP 上高于 raw，MSFI 侧 structure non-worse；但 DiffusionEdge baseline AC 仍比 raw 低 `0.0033`，以当前 `0.003` 容差还差 `0.0003`，false-edge/endpoints 仍 worse than raw，不能写成 Stage1 稳定下游收益；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 114 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P23 `precision_rebalanced_ac_guarded_weak_boundary_fusion_v1` 非正式 downstream-driven 候选：复用 `final.mode=ac_guarded_weak_boundary_bph`，在 P22 基础上向 baseline-side AC/endpoints 约束重调，并保留 `support_unsharp_*` 可选参数但因首次 proxy endpoint 风险将其关闭。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p23_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P23 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P23 MSFI ODS/OIS/AP/AC 为 `0.784316/0.795027/0.346775/0.7943`，DiffusionEdge baseline 为 `0.774055/0.783496/0.364761/0.7941`；结构 proxy vs raw 为 MSFI dF1 `+0.002786`、dFalse-edge `-0.012410`、dEndpoints/kpx `-1.666384`，baseline dF1 `+0.000775`、dFalse-edge `+0.002434`、dEndpoints/kpx `-0.184626`；gate 为 `candidate_metric_near_raw_structure_mixed`。P23 full502 增强指标为 EME `2.5292`、Contrast `25.5004`、AvgGra `2.7276`、MS_SSIM `0.9984`、PSNR `47.8089`、UCIQE `2.2260`、UIQM `7.2777`；compare496 为 EME `2.4225`、Contrast `24.1971`、AvgGra `2.6647`、MS_SSIM `0.9984`、PSNR `47.8155`、UCIQE `2.2503`、UIQM `7.0959`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P23 救回 legacy Final 下游损伤并满足两检测器 metric near-raw，MSFI 侧 structure non-worse；相比 P22 修复 baseline AC near-raw 容差和 endpoint 问题，但 DiffusionEdge baseline false-edge ratio 仍比 raw 高 `+0.002434`，因此不能写成 Stage1 稳定下游收益；本轮未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 115 | 2026-05-26 | 实验/文档 | 新增并执行 P24 `false_edge_floor_ac_guarded_weak_boundary_fusion_v1` 非正式 downstream-driven 候选：复用 `final.mode=ac_guarded_weak_boundary_bph`，在 P23 基础上进一步压低背景/离支撑伪边，不覆盖正式主线配置或正式结果。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p24_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P24 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P24 MSFI ODS/OIS/AP/AC 为 `0.783622/0.794154/0.338141/0.7944`，DiffusionEdge baseline 为 `0.773402/0.782923/0.364694/0.7943`；结构 proxy vs raw 为 MSFI dF1 `+0.0021`、dFalse-edge `-0.0172`、dEndpoints/kpx `-1.3104`，baseline dF1 `+0.0004`、dFalse-edge `+0.0018`、dEndpoints/kpx `-0.3238`；gate 为 `candidate_rescues_legacy_but_not_near_raw`。P24 full502 增强指标为 EME `2.5225`、Contrast `25.4295`、AvgGra `2.7316`、MS_SSIM `0.9984`、PSNR `48.2797`、UCIQE `2.2307`、UIQM `7.2742`；compare496 为 EME `2.4149`、Contrast `24.1189`、AvgGra `2.6682`、MS_SSIM `0.9984`、PSNR `48.2816`、UCIQE `2.2550`、UIQM `7.0911`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P24 改善 Stage1-side proxy 与 baseline false-edge delta，但 MSFI AP 明显回落，整体弱于 P23；不能写成 Stage1 稳定下游收益，未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 116 | 2026-05-26 | 实验/文档 | 新增并执行 P25 `ap_preserving_ac_guarded_weak_boundary_fusion_v1` 非正式 downstream-driven 候选：复用 `final.mode=ac_guarded_weak_boundary_bph`，从 P24 向 P23 回调以保住 MSFI AP，同时保留温和 added-edge/background guard，不覆盖正式主线配置或正式结果。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p25_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P25 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P25 MSFI ODS/OIS/AP/AC 为 `0.783769/0.794450/0.346594/0.7943`，DiffusionEdge baseline 为 `0.773193/0.782810/0.364847/0.7945`；结构 proxy vs raw 为 MSFI dF1 `+0.002119`、dFalse-edge `-0.016326`、dEndpoints/kpx `-1.378609`，baseline dF1 `+0.000227`、dFalse-edge `+0.002878`、dEndpoints/kpx `+0.055596`；gate 为 `candidate_metric_near_raw_structure_mixed`。P25 full502 增强指标为 EME `2.5277`、Contrast `25.4723`、AvgGra `2.7238`、MS_SSIM `0.9985`、PSNR `47.8810`、UCIQE `2.2260`、UIQM `7.2773`；compare496 为 EME `2.4205`、Contrast `24.1705`、AvgGra `2.6609`、MS_SSIM `0.9985`、PSNR `47.8872`、UCIQE `2.2503`、UIQM `7.0943`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P25 修复 P24 的 MSFI AP 回落并保持两检测器 metric-near-raw，但 baseline structure proxy 仍 mixed，不能写成 Stage1 稳定下游收益；未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 117 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P26 `dual_anchor_false_edge_floor_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=dual_anchor_false_edge_floor_bph`，配置为 `experiments/downstream_driven_v1/configs/dual_anchor_false_edge_floor_v1.json`，在 P25-like 弱边界 anchor 上增加 detector-domain dual-anchor false-edge floor，用于压低 off-support 背景、小组件和线状伪边风险，不覆盖正式主线配置或正式结果。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p26_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P26 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P26 MSFI ODS/OIS/AP/AC 为 `0.784101/0.794815/0.346741/0.7937`，DiffusionEdge baseline 为 `0.773399/0.783027/0.364636/0.7953`；结构 proxy vs raw 为 MSFI dF1 `+0.002691`、dFalse-edge `-0.012214`、dEndpoints/kpx `-1.631014`，baseline dF1 `+0.000129`、dFalse-edge `+0.003102`、dEndpoints/kpx `+0.375864`；gate 为 `candidate_rescues_legacy_but_not_near_raw`。P26 full502 增强指标为 EME `2.5452`、Contrast `25.6891`、AvgGra `2.7712`、MS_SSIM `0.9985`、PSNR `48.1879`、UCIQE `2.2262`、UIQM `7.3170`；compare496 为 EME `2.4381`、Contrast `24.3824`、AvgGra `2.7084`、MS_SSIM `0.9985`、PSNR `48.1988`、UCIQE `2.2504`、UIQM `7.1345`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P26 救回 legacy Final 损伤并改善 MSFI 结构 proxy，但 MSFI AC 超出 near-raw 容差，baseline false-edge/endpoints 仍 mixed，不能写成 Stage1 稳定下游收益；未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 118 | 2026-05-26 | 代码/实验/文档 | 新增并执行 P27 `raw_detail_lowfreq_chroma_v1` 非正式 downstream-driven 候选：`main.py` 新增 `final.mode=raw_detail_lowfreq_chroma_bph`，配置为 `experiments/downstream_driven_v1/configs/raw_detail_lowfreq_chroma_v1.json`，采用 raw-detail / low-frequency chroma 机制，保留原图高频与亮度结构，只把低频照明和色度向 BPH 证据轻量迁移，不覆盖正式主线配置或正式结果。完成 168 张 MyEdge split Stage1 输出、MyEdge168 GT edge proxy prescreen 更新到 `docs/stage1_myedge168_gt_edge_proxy_prescreen_p27_20260526_cn.*`，并补跑 502 张 `full502_clean_v1` 输出及 `full502_clean_v1` / `compare9_complete496_v1` 增强指标。MyEdge 侧生成 P27 staging/config/eval `.sh` 后完成固定 MSFI 50k 与 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。P27 MSFI ODS/OIS/AP/AC 为 `0.783329/0.794822/0.346115/0.7951`，DiffusionEdge baseline 为 `0.772587/0.782971/0.372084/0.7949`；结构 proxy vs raw 为 MSFI dF1 `+0.000577`、dFalse-edge `-0.013685`、dEndpoints/kpx `-1.160557`，baseline dF1 `-0.000099`、dFalse-edge `+0.003972`、dEndpoints/kpx `-0.176182`；gate 为 `candidate_metric_near_raw_structure_mixed`。P27 full502 增强指标为 EME `2.5540`、Contrast `26.2208`、AvgGra `2.7684`、MS_SSIM `0.9990`、PSNR `51.0216`、UCIQE `2.2259`、UIQM `7.2528`；compare496 为 EME `2.4436`、Contrast `24.8752`、AvgGra `2.7023`、MS_SSIM `0.9990`、PSNR `50.9524`、UCIQE `2.2503`、UIQM `7.0609`。同步 downstream protocol、README、状态总览、交接指南、`research-state.yaml`，并同步 MyEdge 侧状态文档与日志。边界：P27 救回 legacy Final 损伤并在两个固定检测器上达到 metric-near-raw，MSFI structure non-worse；但 DiffusionEdge baseline false-edge ratio 仍 worse than raw，因此是候选通过而非强通过，不能写成 Stage1 稳定下游收益；未进入 2770 full-pool，未覆盖正式主线结果，未训练新模型。 |
| 119 | 2026-05-26 | 代码/实验/文档 | 新增并执行 D01 `d01_structure_flow_v1` 非正式 `downstream_driven_v2` 候选：`main.py` 新增 `final.mode=downstream_d01_structure_flow_bph`，配置为 `experiments/downstream_driven_v2/configs/d01_structure_flow_v1.json`，将 capped color consistency、低频照明/色度修正、局部对比、结构分支、edge-guided fusion、false-edge suppression、轻量 sharpen 和 bounded selection 做成可开关结构流，不再延续 P12-P28 局部 guard patch，不覆盖正式主线配置或正式结果。完成 168 张 MyEdge split Stage1 输出，Final PNG/JPG 各 `168`；Stage1 proxy prescreen 见 `docs/stage1_myedge168_gt_edge_proxy_prescreen_d01_20260526_cn.*`，D01 为 `proxy_edge_safe_candidate`，dF1 `+0.000590`、dFalse-edge `-0.000672`、dEndpoints/kpx `+0.482890`。MyEdge 侧生成 downstream_v2 staging/config/eval `.sh` 后完成固定 MSFI 50k 与固定 DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、结构 proxy 和 downstream gate。D01 MSFI ODS/OIS/AP/AC 为 `0.783871/0.795067/0.346008/0.7934`，DiffusionEdge baseline 为 `0.771730/0.783218/0.371149/0.7948`；结构 proxy vs raw 为 MSFI dF1 `+0.000343`、dFalse-edge `-0.013767`、dEndpoints/kpx `-1.140514`，baseline dF1 `-0.000327`、dFalse-edge `+0.005265`、dEndpoints/kpx `+0.353641`；strict gate 为 `candidate_rescues_legacy_but_not_near_raw`。新增 `docs/downstream_driven_v2_d01_method_design_and_audit_20260526_cn.md`、`experiments/downstream_driven_v2/protocol.md` 和 D01 stage1 status `.md/.json`，并同步 README、状态总览、交接指南和 `research-state.yaml`。边界：D01 救回 legacy Final 下游崩塌，是 mechanism-complete 弱诊断候选而不是 strong result；MSFI AC 略超 near-raw 容差，baseline false-edge/endpoints 仍 mixed；未运行 `full502_clean_v1`、`compare9_complete496_v1` 或 2770 full-pool，未训练新模型，未修改 checkpoint、GT 或 eval protocol。 |
| 120 | 2026-05-26 | 文档/治理 | 新增 Stage1 实验治理层和 agent guardrails：新增 `experiments/AGENTS.override.md`、`docs/current_experiment_status_cn.md`、`docs/goal_design_contract_cn.md`、`docs/experiment_run_sheet_template_cn.md`、`docs/candidate_lifecycle_policy_cn.md`、`docs/experiment_stop_rules_cn.md`、`docs/stage1_to_myedge_downstream_workflow_cn.md`、`docs/evidence_asset_inventory_cn.md`、`docs/command_environment_rules_cn.md`、`docs/codex_instruction_verification_cn.md`，并新增 `metrics/experiment_registry.csv` 与 `metrics/candidate_registry.csv`。同步 `AGENTS.md`、`README.md`、`docs/project_execution_rules_cn.md`、`docs/project_status_overview_cn.md`、`docs/project_handoff_guide_cn.md` 和 `research-state.yaml`，明确当前优先级是实验治理与候选归档，不是继续派生 Pxx/Dxx；D01 只能写成 `mechanism-complete weak candidate`，`candidate_rescues_legacy_but_not_near_raw`、`candidate_metric_near_raw_structure_mixed`、proxy-only、readiness-only 和 2770 readiness 不能标记为目标完成。边界：本轮未运行增强、训练、MyEdge sampling、WSL `eval.py`、WSL `show.py`、502/496、168 fixed-detector 或 2770 full-pool；未清理、删除、覆盖或回滚 P12-P28/D01 资产。 |
| 121 | 2026-05-27 | 文档/治理 | 执行仓库整理计划，收缩 Stage1Codex 入口文档并明确事实分层：`README.md` 改为一级导航，`docs/project_status_overview_cn.md` 改为当前状态快照，`docs/project_handoff_guide_cn.md` 改为交接摘要，`docs/evidence_asset_inventory_cn.md` 改为证据边界主表，并在 `research-state.yaml` 追加 `repository_reorganization_20260527` 状态块。统一措辞：`locked_full506_final_mainline.json` 与 `full506_final_mainline` 是 formal source asset，不是论文统计 count；论文指标只认 `full502_clean_v1` 与 `compare9_complete496_v1`；P12-P28/D01 是 downstream diagnostic candidates / archived evidence；D01 是 mechanism-complete weak diagnostic candidate，不是 strong pass；2770 是 cv2-readable full-pool candidate / qualitative engineering pool，不是正式结果；Wu 2026 两篇论文是 nearest-neighbor / overlap-risk anchor，数据 overlap 未证明。边界：本轮未运行实验、未改代码、未新增 Pxx/Dxx、未进入 2770、未覆盖正式结果、未移动或删除历史资产。 |
| 122 | 2026-05-27 | 文档/治理 | 执行第一批治理提交前的 blocker 修正：修复 `metrics/experiment_registry.csv` 中 P28 行字段数错位，新增 `metrics/registry_schema_cn.md` 定义 experiment/candidate registry 字段、枚举和提交前校验规则，并将 schema 入口同步到 `AGENTS.md`、`README.md`、`docs/project_execution_rules_cn.md` 与 `research-state.yaml`。同时收敛 D01/P27 相关容易误导的通过类措辞，统一为 weak diagnostic / metric-near-raw diagnostic 边界。边界：本轮只做治理文档和 registry 修正，未运行增强、训练、MyEdge sampling、WSL `eval.py`、WSL `show.py`、502/496、168 fixed-detector 或 2770 full-pool；未修改 `main.py`、未清理候选资产、未处理删除替代闭环。 |
| 123 | 2026-05-27 | 文档/治理 | 执行 Stage1Codex 第二批状态层审计：将已推送的治理层提交作为当前远程规则来源，并在 `research-state.yaml` 中把 D01/P13/P14/P26/P27 等候选统一表述为 diagnostic / weak diagnostic / not strong result，避免把 near-raw、mixed gate 或 legacy-rescue 误写成正式成功。`research-log.md` 继续保持 append-only；历史条目中的旧措辞按当时记录保留，但当前项目口径以 `research-state.yaml`、`docs/current_experiment_status_cn.md`、`docs/goal_design_contract_cn.md`、registry 和本条日志为准。边界：本轮只做状态层同步，未运行增强、训练、MyEdge sampling、WSL `eval.py`、WSL `show.py`、502/496、168 fixed-detector 或 2770 full-pool；未修改 `main.py`，未提交实验输出资产。 |
| 124 | 2026-05-27 | 文档/纠偏 | 新增 `docs/stage1_full_enhancement_mainline_recovery_plan_cn.md`，把当前目标从“继续 raw-near / guard / fallback / raw-pullback 小候选”纠正为“恢复完整创新型 Stage1 增强主线并验证 downstream 正收益”。同步 `docs/current_experiment_status_cn.md`、`README.md` 与 `research-state.yaml`，明确 D01 没有达成原始完整增强主线目标：未运行 502/496 外部增强对比、没有明确稳定 downstream 正收益、视觉结果接近 raw、不能写成类似参考论文的完整创新增强流程。新的长期目标暂定为 `full_flow_downstream_stage1_mainline_v1`，后续必须先写 method design 和 run sheet，再进入代码、smoke、168 fixed-detector validation、502/496 或 2770 readiness。边界：本轮未运行实验、未改增强代码、未新增候选 ID、未覆盖正式结果或候选资产。 |
| 125 | 2026-05-27 | 文档/计划 | 落地 Phase 1 方法设计与 run sheet：新增 `docs/full_flow_downstream_stage1_mainline_v1_method_design_cn.md` 和 `experiments/full_flow_downstream_stage1_mainline_v1/run_sheet_v1.md`。新主线计划恢复完整 Stage1 创新骨架：degradation diagnosis、gray-pixel color formation、IMF/frequency detail branch、WB-safe contrast branch、local visibility branch、downstream-aware gated fusion、bounded filtering/illumination closure。run sheet 明确该路线不是 D01/P27 的 raw-near 小回调，后续若实施需新增 isolated module/config/output root，并先做 smoke/visual gate，再做 168 fixed-detector validation；502/496 和 2770 均保持后置边界。边界：本轮未运行实验、未改增强代码、未生成配置、未新增输出资产、未运行 MyEdge sampling/eval/show、未处理 502/496 或 2770。 |
| 126 | 2026-05-27 | 代码/计划 | 推进 Phase 2 隔离代码入口：新增 `stage1_full_flow_mainline.py` 和 `experiments/full_flow_downstream_stage1_mainline_v1/configs/full_flow_downstream_stage1_mainline_v1.json`，并在 `main.py` 中加入显式 `final.mode=full_flow_downstream_stage1_mainline_v1` dispatch。新入口保留 BPH、IMF/detail、WB-safe contrast、CLAHE/local visibility、多分支融合和 bounded final closure 的完整增强骨架，只在显式配置下运行，不改变默认正式主线、不覆盖 locked config 或正式输出。边界：本轮只新增可编译代码入口和计划配置，未运行增强实验、未处理图像、未运行 MyEdge sampling/eval/show、未重跑 502/496、未进入 2770。 |
| 127 | 2026-05-27 | 代码/实验/诊断 | 对 `full_flow_downstream_stage1_mainline_v1` 执行 5 张真实图像 smoke：manifest 为 `experiments/full_flow_downstream_stage1_mainline_v1/manifests/smoke5_v1.txt`，输出到 `experiments/full_flow_downstream_stage1_mainline_v1/outputs/smoke5_v1`，6 个 stage 的 JPG/PNG 均为 `5/5` 且 decode 失败 `0`。实测耗时约 `179.9` 秒，约 `35.98` 秒/张，外推 168 张约 `100.7` 分钟，显著超过 10 分钟目标；smoke 指标显示 Final 相对 raw 平均 BGR delta `8.7569`、L delta `9.6795`、梯度均值比 `1.6948`，说明不是 near-raw，但 `tama.14` 梯度比达到 `3.397` 且 panel 出现块状/背景伪边风险。因此新增 bounded output selection 参数与实现，将最终输出按预设强度向 BPH 安全锚回收以约束全局梯度、亮度方差和平均亮度漂移。边界：本轮未运行 168 fixed-detector、未运行 MyEdge sampling/eval/show、未重跑 502/496、未进入 2770；当前结论是 smoke 完成但禁止进入 168 gate，需先复跑 smoke 并做人眼 panel 复核。 |
| 128 | 2026-05-27 | 代码/实验/诊断 | 继续推进 `full_flow_downstream_stage1_mainline_v1` smoke 纠偏并形成当前 v8 停止点：将 aggressive IMF 改为 fast IMF；新增 robust edge confidence、branch support floor、bounded selection、`direct_weighted` 融合后端和 smoke summarizer；保留 legacy Laplacian fusion 与 entropy final closure 作为可消融后端，但默认关闭 entropy closure，因为 v7 证明它会重新引入 `tama.14` 背景伪结构。当前采用 `smoke5_fast_imf_v8`：输出根为 `experiments/full_flow_downstream_stage1_mainline_v1/outputs/smoke5_v1_fast_imf_v8`，5 张 x 6 stages 的 JPG/PNG 均完整且 decode 失败 `0`，耗时约 `3.0` 秒，外推 168 约 `1.7` 分钟；Final 平均 BGR delta `6.8792`、L delta `1.0740`、grad ratio `1.0628`、luma std ratio `1.0273`，`tama.14` Final grad ratio 降至 `1.2095`。同步 run sheet、当前状态和 `research-state.yaml`。边界：v8 只是 Stage1-only smoke，不是 168 fixed-detector 下游证据；未运行 MyEdge sampling/eval/show、502/496 或 2770；下一步必须先做 20-30 张 broader smoke 和人工 panel 复核，再决定是否进入 168。 |
| 129 | 2026-05-27 | 实验/诊断 | 执行 `full_flow_downstream_stage1_mainline_v1` 的 25 张 broader Stage1-only smoke：新增 manifest `experiments/full_flow_downstream_stage1_mainline_v1/manifests/smoke25_v1.txt`，输出到 `experiments/full_flow_downstream_stage1_mainline_v1/outputs/smoke25_v1_fast_imf_v8`，25 张 x 6 stages 的 JPG/PNG 均完整且 decode 失败 `0`。实测耗时约 `11.6` 秒，约 `0.46` 秒/张，外推 168 约 `1.3` 分钟；Final 平均 BGR delta `7.0538`、L delta `1.0849`、grad ratio `1.0366`、luma std ratio `1.0443`，最高 grad ratio 为 `tama.13` 约 `1.099`。抽查最高梯度与最低增强 panel 后，未见 v1-v7 的大块背景伪结构，但视觉增强偏保守，主要体现为颜色/色度一致性和轻量结构保持。同步 run sheet、当前状态与 `research-state.yaml`。边界：本轮未运行 168 fixed-detector、MyEdge sampling/eval/show、502/496 或 2770；当前结论只是允许进入 168 enhancement preflight，不是下游正收益证据。 |
| 130 | 2026-05-27 | 实验/诊断/治理 | 完成 `full_flow_downstream_stage1_mainline_v1_v8` 的 168 fixed-detector 闭环并纠正状态：168 张 Stage1 输出 Final PNG/JPG 各 `168`，decode 失败 `0`，耗时约 `74.9` 秒；随后在 MyEdge 侧完成 fixed MSFI 50k 与 fixed DiffusionEdge baseline 50k sampling、WSL `.sh` eval/show、result intake、structure proxy 和 downstream gate。FF01/v8 MSFI ODS/OIS/AP/AC 为 `0.739726/0.753251/0.310442/0.790100`，DiffusionEdge baseline 为 `0.717475/0.727626/0.336548/0.794400`，相对 raw 均明显下降；structure proxy 两路 detector 均 worse than raw，MSFI dF1 `-0.053426`、dFalse-edge `+0.079593`、dEndpoints `+1.891529`，baseline dF1 `-0.067472`、dFalse-edge `+0.095084`、dEndpoints `+4.291561`。gate 为 `candidate_rescues_legacy_but_not_near_raw`，低于 P27/P28 的 metric-near-raw mixed 证据，也弱于或不优于 D01。新增 `experiments/full_flow_downstream_stage1_mainline_v1/full_flow_downstream_stage1_mainline_v1_fixed_detector_v8_status_20260527.md`，同步 run sheet、README、当前状态、registry 和 `research-state.yaml`。边界：FF01/v8 是完整流程原型的失败/诊断证据，不是完整增强主线成功；不得进入 502/496 或 2770 作为 candidate-passing route；未修改 MyEdge checkpoint、GT、eval protocol 或正式 Stage1 主线资产。 |
| 131 | 2026-05-27 | 实验/诊断/治理 | 完成 `full_flow_downstream_stage1_mainline_v2` / FF02 的 168 fixed-detector 闭环并收口纠偏：FF02 是 FF01 失败后的 detector-compatible 机制级重设计，保留灰像素、IMF/频域、多分支融合、color/structure decoupling、topology-compatible fusion 和 bounded closure。168 张 Stage1 输出 Final PNG/JPG 各 `168`，decode 失败 `0`，耗时约 `77.5` 秒；Final mean BGR/L/chroma delta 为 `11.7138/1.3708/7.9785`，grad ratio `0.9450`，luma std ratio `1.0132`。MyEdge fixed MSFI 50k 为 `0.737952/0.751109/0.303208/0.792000`，fixed DiffusionEdge baseline 50k 为 `0.711020/0.720141/0.320951/0.794000`，均明显低于 raw；structure proxy 两路 detector 均 worse than raw，MSFI dF1 `-0.0507`、dFalse-edge `+0.0734`、dEndpoints `+2.0630`，baseline dF1 `-0.0666`、dFalse-edge `+0.0890`、dEndpoints `+3.7585`。gate 仍为 `candidate_rescues_legacy_but_not_near_raw`；FF02 比 FF01 稍降结构风险但 ODS/OIS/AP 更差。新增 `experiments/full_flow_downstream_stage1_mainline_v2/full_flow_downstream_stage1_mainline_v2_fixed_detector_ff02_status_20260527.md`，同步 method design、run sheet、README、当前状态、恢复计划、registry 和 `research-state.yaml`。边界：FF02 是完整流程重设计后的 rescue-only diagnostic failure，不是下游正收益；不得进入 502/496 或 2770 作为 candidate-passing route；停止同族 full-flow FF03 小修，下一步必须先做 failure audit 或转向 MyEdge/MSFI 侧适配。 |
| 132 | 2026-05-27 | 分析/治理 | 启动并完成 FA01 WP1 family-level failure audit，同时预注册 WP2 detector-sensitivity hypotheses。新增 `docs/stage1_full_flow_family_failure_audit_fa01_20260527_cn.md`、`docs/stage1_detector_sensitivity_hypotheses_fa01_20260527_cn.md`，并生成只读聚合表 `docs/fa01_stage1_full_flow_family_audit_tables_20260527/fa01_fixed_detector_metrics.csv`、`fa01_structure_proxy_metrics.csv`、`fa01_enhancement_proxy_168_summary.csv`、`fa01_enhancement_proxy_168_per_image.csv`、`fa01_502_496_boundary.csv` 与 `fa01_asset_index.json`。审计结论：legacy Final 是严重负向 baseline；P27/D01 能接近 raw 但增强幅度太低，不能满足完整增强流程目标；FF01/FF02 更接近完整增强流程但 fixed-detector ODS/AP 和 structure proxy 均明显低于 raw；FF02 mean grad ratio 低于 1 仍更差，说明失败不只是全局梯度放大，更可能涉及色度/低频分布迁移、局部 topology drift 与 detector raw-distribution bias。同步 README、AGENTS、当前状态、项目总览、证据索引和 `research-state.yaml`。边界：本轮未运行新增强、MyEdge sampling、WSL eval/show、训练、502/496 重算或 2770；FA01 是诊断分析，不是新候选或正收益证据；完成前不得创建 FF03/P29/D02。 |
| 133 | 2026-05-27 | 分析/计划/治理 | 继续推进 FA01 WP2 只读 detector-sensitivity 审计：新增 high-risk sample evidence index `docs/fa01_high_risk_sample_evidence_index_20260527_cn.md` 与路径表目录 `docs/fa01_high_risk_sample_index_20260527/`，覆盖 57 个 high-risk/adverse stems，并记录 raw、GT、Stage1 输出、MyEdge white/overlay/error_map/mat-eval/nms-eval 路径；新增 per-image correlation audit `docs/fa01_per_image_correlation_audit_20260527_cn.md` 与表目录 `docs/fa01_per_image_correlation_audit_20260527/`，合并 enhancement proxy 与 structure proxy，生成相关表和 top adverse cases。结果显示单一增强 proxy 对 FF01/FF02 失败解释不足，FF02 在 mean grad ratio 低于 1 时仍更差，false-edge adverse cluster 包括 `weixiaoyuanjia.26`、`xuehong.9/13/11`、`donghaiyuanjia.26`、`weixiaoyuanjia.21`。同时新增 `docs/myedge_msfi_stage1_sidecar_adaptation_protocol_fa01_20260527_cn.md`，把下一步从 fixed-detector image replacement 转为 raw 主输入 + Stage1 sidecar / dual-input / weak-boundary evidence 的 MyEdge/MSFI adaptation 草案。同步 README、AGENTS、当前状态、项目总览、证据索引和 `research-state.yaml`。边界：本轮未运行增强、MyEdge sampling、WSL eval/show、训练、502/496 重算或 2770；adaptation protocol 只是草案，不授权训练，不是 fixed-detector 正收益证据；仍不得创建 FF03/P29/D02。 |
| 134 | 2026-05-27 | 分析/准备/治理 | 继续推进 FA01 审计的可视化与 sidecar 准备：新增 `metrics/scripts/build_fa01_visual_error_map_review.py`，只读 high-risk 路径索引与 per-image proxy 表，生成 `docs/fa01_visual_error_map_review_20260527_cn.md`、`docs/fa01_visual_error_map_review_20260527/fa01_visual_error_map_review_index.json`、panel/path CSV、pattern tag CSV 和 11 个 high-risk visual/error-map panel；结论是 FF01/FF02 直接替换 raw 的失败表现为 boundary F1 loss、false-edge increase、endpoint fragmentation、luma/detail topology drift 与 color/chroma shift 的组合。新增 `metrics/scripts/export_fa01_stage1_sidecar_maps.py` 与 `docs/stage1_sidecar_map_definition_fa01_20260527_cn.md`，对 5 个 high-risk 样本完成 no-training sidecar export smoke，输出 35 张 topology/color/frequency/visibility/risk/weak-boundary maps、5 张 panel、manifest 和 metadata。同步 README、AGENTS、当前状态、项目总览、执行规则、证据索引和 `research-state.yaml`。边界：本轮未创建新 Pxx/Dxx/FFxx 候选，未运行 MyEdge sampling、WSL eval/show、训练、checkpoint 修改、502/496 重算或 2770；visual review 与 sidecar smoke 只能写成诊断/导出准备，不是 fixed-detector 正收益或 adaptation 结果。 |
| 135 | 2026-05-27 | 代码/实验/纠偏/治理 | 完成 TLVC01 `topology_locked_visual_chroma_full_flow_v1` 纠偏候选闭环：新增 topology-locked visual-chroma 完整流程入口、配置、run sheet、通用 MyEdge single-candidate result intake 脚本 `metrics/scripts/sync_myedge_single_candidate_result_intake.py`，并修正关键协议事实：168 direct-replacement candidate 必须从 `D:/Desktop/MyEdgeCodex/input_test/algae` 生成，不能用 Stage1 `data/inputImg/Original` raw copy 替代 MyEdge raw anchor。TLVC01 168 输出 Final PNG/JPG 各 `168`，decode 失败 `0`，耗时约 `92.9` 秒；Final mean BGR/L/chroma delta 为 `10.7667/1.7113/7.3842`，grad ratio `0.9956`，luma std ratio `1.0000`；Stage1/MyEdge168 GT edge proxy 与 raw 完全一致。fixed MSFI 50k 为 `0.782936/0.794718/0.345209/0.793200`，fixed DiffusionEdge baseline 50k 为 `0.768593/0.779518/0.362225/0.798500`；structure proxy 为 MSFI non-worse，但 DiffusionEdge baseline mixed：dF1 `-0.0030`、dFalse-edge `+0.0047`、dEndpoints `+0.3836`。downstream gate 为 `candidate_rescues_legacy_but_not_near_raw`，TLVC01 比 FF01/FF02 安全但没有超过 P27/D01 的 DiffusionEdge AP 证据，不是下游正收益。新增 `experiments/topology_locked_visual_chroma_full_flow_v1/topology_locked_visual_chroma_full_flow_v1_fixed_detector_tlvc01_status_20260527.md` 和 `docs/stage1_long_horizon_goal_after_tlvc01_20260527_cn.md`，同步 README、当前状态、证据索引、registry 和 `research-state.yaml`。边界：未训练/改动 MyEdge checkpoint，未改 GT 或 eval protocol，未覆盖正式 Stage1 主线，未运行 502/496 或 2770；停止 TLVC02/FF03/P29/D02 式 direct-replacement 小修，下一步转向 raw 主输入 + Stage1 sidecar evidence + MyEdge/MSFI adaptation 的单独授权路线。 |
